# 사용과 개선

`/오늘` 을 실제로 쓰고, 안 되는 것을 찾아 고친 기록.

---

## 1회차 — 2026-09-24 15:00

기록 파일: [실행기록/2026-09-24.md](내과제/실행기록/2026-09-24.md)

**입력**: 가용시간 9시간 (15:00 → 마감 00:00)

**기대한 결과**
7과목에 흩어진 과제가 한 목록으로 모이고, 오늘 할 수 있는 것만 골라져 나올 것.

**실제 결과**

```
이캠퍼스 일정 18건 수집
  → 과제 4건 / 강의차시 11건 / 비교과·제외 3건

오늘 할 것
1. [오늘] 네트워크서비스 · 나만의 자비스 만들기 — 9h
   마감이 9시간 뒤. 우선순위 규칙 ①에 걸려 1순위. 남은 시간을 전부 쓴다.

오늘 안 함
- 머신러닝 3주차 (D-4), 네트워크서비스 10-07 과제 2건 (D-13)
```

수집과 분류는 기대대로 됐다. **18건 중 진짜 과제는 4건**이었고, 나머지는 강의 차시와
외부 특강이었다. 손으로 옮겨 적을 필요가 없어진 게 가장 큰 변화다.

그런데 화면을 보다가 두 가지가 잘못됐다는 걸 알았다.

---

## 문제 1 — 필요한 정보를 "과제가 아니다"라며 버렸다

### 어떻게 발견했나

1회차 출력의 마지막 줄이 이랬다.

```
## 제외됨 — 강의차시 11건, 비교과 3건
```

개수만 있고 내용이 없다. 그런데 그 11건 안에 **네트워크서비스 `4-1` ~ `4-9` 동영상 강의**가
들어 있었다. 금요일 수업 전에 봐야 하는 것들이다.
**제출물이 아니라고 해서 안 해도 되는 일은 아니었다.**

원래 설계가 "과제냐 아니냐"의 2분법이었던 게 문제였다. 실제로 내 하루에는 트랙이 둘이다 —
**내야 하는 것**과 **봐야 하는 것**.

### 무엇을 고쳤나

| | 개선 전 | 개선 후 |
|---|---|---|
| 강의차시 | `제외됨 11건` — 개수만 | **`당장 봐야 할 강의` 트랙으로 분리.** 제목·과목·D- 전부 표시 |
| 분류 | 과제 / 나머지 | 과제 / 강의차시 / 비교과 / 제외과목 **4갈래** |
| 출력 | 텍스트만 | `--html` 로 **대시보드 생성** 추가 |

고친 파일: `내과제/일정가져오기.py`, `.claude/skills/오늘/SKILL.md`

### 개선 후 결과

```
## 강의차시 (11건)
| 2026-09-25 | 내일 | 네트워크서비스 | 4-1 오늘은 중간에 밖으로 나갑니다 |
| 2026-09-25 | 내일 | 네트워크서비스 | 4-2 ask로 다듬고, 기준을 먼저 정하기 |
...
| 2026-09-22 | 지남 2일 | 게임프로그래밍 | 4주차 Stack |
```

**내일 봐야 할 동영상 9개가 보인다.** 그리고 덤으로 게임프로그래밍·인터랙티브
2건이 **2일 지났다**는 것도 드러났다. 개선 전에는 둘 다 안 보였다.

---

## 문제 2 — 강의 날짜가 하루씩 당겨졌다

### 어떻게 발견했나

문제 1을 고쳐서 강의차시를 화면에 꺼냈더니 `4-1` ~ `4-9` 가 **09-24(오늘)** 로 찍혔다.
그런데 네트워크서비스는 **금요일(09-25)** 수업이다. 하루가 어긋났다.

원인: iCal 에서 **시각이 붙은 이벤트는 UTC 로 온다.**
`20260924T150000Z` 는 한국시간으로 **09-25 00:00** 인데, 앞 8자만 잘라서 `20260924` 로 읽었다.
날짜만 있는 과제 마감은 현지 날짜라 멀쩡했고, 그래서 **강의차시에서만** 티가 났다.

### 무엇을 고쳤나

`to_kst_date()` 를 새로 만들어, 시각이 있는 이벤트는 UTC → KST 로 옮긴 뒤 날짜를 뽑게 했다.
날짜만 있는 이벤트는 변환하지 않는다.

### 개선 후 결과 — 같은 데이터, 같은 명령

| 항목 | 개선 전 | 개선 후 | 실제 |
|---|---|---|---|
| 네트워크서비스 4-1~4-9 | 09-24 (오늘) | **09-25 (내일)** | 금요일 수업 ✓ |
| 게임프로그래밍 4주차 | 09-21 (지남 3일) | **09-22 (지남 2일)** | 화요일 수업 ✓ |
| 인터랙티브 5주차 | 09-21 | **09-22** | 화요일 수업 ✓ |
| 과제 4건 | 변화 없음 | 변화 없음 | 날짜만 있어 영향 없음 ✓ |

시간표와 대조해서 검증했다. 세 과목 모두 **실제 수업 요일과 일치**한다.

> 이건 화면에 안 꺼냈으면 영영 못 찾았을 버그다.
> 문제 1을 고친 덕분에 문제 2가 보였다.

---

## 2회차 — 2026-09-24 15:31

기록 파일: [실행기록/2026-09-24.md](내과제/실행기록/2026-09-24.md) 의 "2회차" 절

**입력**: `/오늘 2시간`

> **조건을 밝힌다** — 이 2회차는 1회차와 **같은 대화**에서 돌렸다. 스킬이 파일을 직접
> 읽어서 판단한 것은 맞지만, "새 대화가 기록만으로 이어받는다"의 완전한 증명은 아니다.
> 그 검증은 아래 3회차에서 한다.

**기대한 결과**

1회차가 "다음 실행에 넘기는 것"에 적어둔 대로 머신러닝 3주차(D-4)를 1순위로 올리고,
소요시간을 먼저 물을 것.

**실제 결과 — 기대와 달랐고, 그게 맞았다**

```
오늘 (2026-09-24 목, 15:31 · 가용 2시간)

1. [오늘 마감 · 8시간 29분 남음] 네트워크서비스 · 나만의 자비스 만들기
   (이어받음, 같은 날 2회차) — 남은 작업 약 1시간
   우선순위 규칙 ①에 걸린다. 남은 건 제출 절차뿐이다.

오늘 안 함
- 3주차 머신러닝 과제 (D-4) — 예상소요 미상. 오늘 가용시간은 마감 건이 다 쓴다
```

머신러닝을 올리지 않았고 **소요시간도 묻지 않았다.** 처음엔 실패로 보였는데, 근거를 보니
규칙대로였다. 1회차의 메모는 *오늘 마감 건이 끝난 뒤*를 전제한 것이었고, 오늘 마감이
아직 남은 상태에서는 규칙 ①이 이긴다. 스킬의 *"오늘 제안에 영향을 주지 않는 질문은 하지
않는다"* 도 그대로 지켜졌다.

**기록을 실제로 읽었다는 증거**

- [x] 1회차가 넘긴 세 항목을 표로 대조했다
- [x] `(이어받음, 같은 날 2회차)` 로 표시했다 — 새 항목으로 다시 올리지 않았다
- [x] 소요시간을 **추정하지 않고** 확인 대기로 남겼다
- [x] 가용시간(2시간) 안에서만 제안했고, 3개를 넘기지 않았다
- [x] **1회차 기록과 마감목록의 불일치를 잡아냈다** — 아래 문제 3

---

## 문제 3 — 기록 두 개가 어긋나도 아무도 알려주지 않는다

### 어떻게 발견했나

2회차가 두 파일을 읽자마자 나왔다.

| 파일 | `나만의 자비스 만들기` 상태 |
|---|---|
| `실행기록/2026-09-24.md` | `[x]` 완료 |
| `마감목록.md` | `진행중` |

1회차가 실행기록에만 완료를 찍고 마감목록은 그대로 뒀다. 실제로는 제출 전이라
**`진행중` 이 맞았다.** 두 파일을 다 읽었기 때문에 잡혔지, 한쪽만 읽었으면 끝난 과제로
넘어가고 오늘 마감을 놓쳤을 것이다.

설계할 때 *"한쪽만 고치면 다음 실행이 어긋난다"* 고 스킬에 적어놓고도, 정작 1회차에서
그걸 어겼다. **규칙을 적어두는 것만으로는 안 지켜진다.**

### 무엇을 고쳤나

`SKILL.md` 2절에 **"두 파일이 어긋나면 먼저 멈춘다"** 를 넣었다.

- 실행기록의 완료 표시와 마감목록의 상태를 **매번 대조한다**
- 어긋나면 **어느 쪽이 맞는지 내가 정하지 않고 사용자에게 묻는다**
- 확인받은 뒤 두 파일을 같이 고치고, **정정했다는 사실을 남긴다.** 지우고 덮어쓰지 않는다

### 개선 후 결과

2회차가 불일치를 보고했고, 1회차 표의 `[x]` 를 `[ ]` 로 정정하면서 그 아래에
정정 사유를 남겼다. 기록을 고친 게 아니라 **고쳤다는 기록을 더했다.**

> 기록이 두 벌이면 언젠가 어긋난다. 어긋나는 걸 막을 수는 없으니 **발견하는 절차**를 둔다.
> 이게 이 과제에서 가장 크게 배운 것이다.

---

## 3회차 — 2026-09-24 15:36

기록 파일: [실행기록/2026-09-24.md](내과제/실행기록/2026-09-24.md) 의 "3회차" 절

**입력**: `/오늘 1시간`

**기대한 결과** — 2회차에서 넣은 대조 절차가 실제로 도는지.

**실제 결과**

```
기록 대조
  실행기록 2회차 [ ] 미완료  vs  마감목록 진행중   → 일치. 통과

오늘 (가용 1시간)
1. [오늘 마감 · 8시간 23분] 나만의 자비스 만들기 (이어받음, 3회차) — 0.5h

확인 필요 — 오늘 판단에 영향을 준다
- 머신러닝 3주차 과제 (D-4) 예상 소요시간
```

**2회차가 틀렸던 것을 3회차가 잡았다.** 2회차는 머신러닝 소요시간을 묻지 않으면서
"오늘 가용시간은 마감 건이 다 쓴다"고 적었는데, 다시 보면 **그때도 2시간 중 1시간이
남아 있었다.** 물었어야 했다. 3회차는 1시간 중 30분이 남아 같은 질문이 오늘 판단에
영향을 주므로 **물었고**, 답(1시간)을 받아 `마감목록.md` 에 적었다.

같은 규칙이 상황에 따라 다른 답을 내는 게 정상이다. 기록이 남아 있었기 때문에
**앞 회차의 느슨한 판단을 뒤 회차가 잡아낼 수 있었다.**

---

## 문제 4 — 강의 날짜를 마감으로 읽었다

### 어떻게 발견했나

화면을 보다가 이상했다. 게임프로그래밍 4주차가 **"지남 2일"** 로 떠 있는데,
실제로는 아직 볼 수 있는 강의였다.

원인: **이캠퍼스는 강의 차시에 날짜를 두 개 준다.**

| 항목 | 뜻 | 내가 한 것 |
|---|---|---|
| `DTSTART` | 시청이 **열리는** 날 | 이걸 마감으로 읽었다 |
| `DTEND` | 시청이 **닫히는** 날 | **아예 안 봤다** |

09-22 에 *열린* 강의를 09-22 가 *마감*인 줄 알고 "지남 2일"이라고 말한 것이다.
실제 닫히는 날은 10-06, 열이틀이나 남아 있었다.

**문제 1·2 를 고치면서 강의를 화면에 꺼냈기 때문에 보였다.** 숨겨져 있었으면
학기 내내 몰랐을 것이고, 반대로 "이미 지났다"고 믿고 안 봤을 수도 있다.

### 무엇을 고쳤나

| | 개선 전 | 개선 후 |
|---|---|---|
| 수집 | `DTSTART` 만 | `DTEND` 도 읽어 `closes` 로 보관 |
| 스크립트 표 | `마감 \| D- \| 과목 \| 제목` | `열림 \| 열림까지 \| 과목 \| 제목 \| 닫힘` |
| 화면 | 열리는 날에 세움 | **닫히는 날(마감) 칸에 세움** |
| 상태 | 열림/지남 2분법 | 아직 안 열림 / 볼 수 있음 / 닫힘 |

고친 파일: `내과제/일정가져오기.py`, `deploy/src/worker.js`, `내과제/웹앱.html`,
`.claude/skills/오늘/SKILL.md`, `deploy/schema.sql`

서버에는 `ALTER TABLE items ADD COLUMN closes TEXT` 를 한 번 돌리고 재배포한 뒤,
동기화로 기존 11건의 `closes` 를 채웠다.

### 개선 후 결과 — 시간표와 대조

| 항목 | 개선 전 | 개선 후 | 실제 |
|---|---|---|---|
| 게임프로그래밍 4주차 | 지남 2일 | **볼 수 있음 D-12** | 10-06 까지 ✓ |
| 인터랙티브 5주차 | 지남 2일 | **볼 수 있음 D-12** | 10-06 까지 ✓ |
| 네트워크서비스 4-1~4-9 | 내일 마감처럼 | **내일 열림, 10-09 까지** | 금요일 수업 ✓ |
| 과제 4건 | 변화 없음 | 변화 없음 | 날짜만 있어 영향 없음 ✓ |

`node deploy/test.mjs` 로 파이썬·Worker 결과가 같은지 검증했다. **14/14 통과.**

> 세 번째로 같은 자리에서 틀렸다. 문제 2도, 문제 4도 **날짜를 잘못 읽은 것**이다.
> 달력 데이터는 "그냥 날짜"처럼 보이지만 실제로는 시간대·시작·종료가 얽혀 있다.
> 눈으로 한 번 맞다고 본 것을 믿지 말고 **실제 수업 요일과 대조**해야 한다.

---

## 만들었다가 되돌린 것 — 화면에 넣은 판단

"VS Code 안 켜고 웹에서만 끝내고 싶다"는 요구가 나와서, 「계획 짜기」를 화면에 넣었다.
가용시간을 적으면 최대 3개를 근거와 함께 뽑아 주는, `/오늘` 과 같은 기능이다.

**그런데 그러려면 우선순위 규칙을 자바스크립트로 다시 써야 했다.** 웹에는 Claude 가
없어 `SKILL.md` 를 읽을 주체가 없기 때문이다. 같은 규칙이 두 벌이 되고,
한쪽만 고치면 **대화와 화면이 다른 답을 낸다.** 사용자는 어느 쪽이 맞는지 알 수 없다.

그래서 처음에는 "막을 수 없으니 드러나게" 하는 쪽으로 갔다 — 앞의 세 번과 같은 처방으로
`SKILL.md` 와 `CLAUDE.md` 에 "고치면 두 곳을 같이"를 박았다.

**그다음 화면이 복잡하다는 말이 나와서 그 기능을 걷어냈다.** 구역이 여덟 개까지
늘어나 있었고, 계획 패널이 그중 하나였다. 빼고 나니 두 벌 문제도 같이 사라졌다.

| | 넣었을 때 | 되돌린 뒤 |
|---|---|---|
| 판단(가용시간·3개 제한) | 스킬 + 화면 **두 벌** | `/오늘` **한 벌** |
| 화면이 하는 일 | 보여주기 + 판단 | 보여주기만 |
| 구역 수 | 8개 | 2개 |

**기능을 더하는 것이 언제나 개선은 아니다.** 되돌리고 나서야 화면이 읽혔고,
규칙도 한 곳에만 남았다. 다만 지침 두 곳에 남아 있던 "두 곳을 같이 고쳐라" 경고를
**한동안 안 지웠다** — 문서가 실물보다 뒤처지는 같은 실수를 또 한 것이다.
발견하고 세 파일을 같이 맞췄다.

## 남은 문제

- **마감 시각을 못 읽는다.** 이캠퍼스 일정에는 날짜만 온다.
  `나만의 자비스 만들기` 가 09-25 00:00 마감이라는 건 사람이 직접 확인해서 넣었다.
  대시보드는 23:59 로 가정해 표시하고, 가정이라는 걸 화면에 밝혀 둔다.
- **디지털트윈프로그래밍·단기 K-프로젝트I 은 이캠퍼스에 일정이 없다.**
  자동 수집으로 못 메운다. 주 1회 수동 확인이 필요하고, 이 한계를 `과목.md` 에 적어두었다.
- **과제 설명 본문을 못 읽는다.** 이캠퍼스가 SSO 라서 API 토큰 발급이 막혀 있다
  (`login/token.php` → `redirecterrordetected`). 마감일까지만 자동이고 내용은 사람이 본다.


---

# 마무리 질문

## 자비스에게 맡긴 일과 내가 직접 판단하는 일

경계는 한 줄로 정해졌다. **모르는 값을 지어내야 하는 순간이 경계다.**
계산은 맡기고 추정은 맡기지 않는다.

| 맡긴 일 (계산) | 내가 하는 일 (판단) |
|---|---|
| 이캠퍼스에서 일정을 받아 과제·강의·비교과로 가르기 | 분류가 틀렸을 때 정정하기 |
| 마감까지 남은 일수, 남은 시간 대비 여유 | **예상 소요시간이 몇 시간인지** |
| 어제 미완료 항목 이어받기 | 같은 날 마감이 겹칠 때 **무엇을 먼저 할지** |
| 가용시간 초과 여부 계산 | **포기하거나 미룰지** |
| 실행기록·마감목록에 남기기 | 두 기록이 어긋났을 때 **어느 쪽이 맞는지** |

이 경계를 `CLAUDE.md` 의 「사용자에게 물어야 하는 결정」과 `SKILL.md` 의
「내가 판단하는 것 vs 사람이 판단하는 것」 표에 글로 박아두었다.
**표로 박지 않으면 AI가 "알아서" 판단해 버린다.**

## 새 대화에서 이어서 일하려면 필요했던 정보

**한 일만으로는 부족했다. 안 한 일과 그 이유가 있어야 했다.**

1회차가 "머신러닝은 오늘 마감 건에 시간을 다 써서 안 함, 다음 1순위" 를 남겼기 때문에
2회차가 그걸 이어받을 수 있었다. 한 일만 적혀 있었으면 2회차는
머신러닝을 **처음 보는 항목처럼** 다시 올렸을 것이다.

그래서 실행기록에 세 가지를 반드시 남긴다.

- 오늘 한 것과 **완료 여부**
- **오늘 안 한 것과 그 이유**
- **다음 실행에 넘기는 것**

여기에 하나가 더 필요했다. 기록이 두 벌(`실행기록` · `마감목록.md`)이라
**대조 절차**가 없으면 어긋난 채로 진행된다. 실제로 1회차에서 어긋났다.

## 매뉴얼을 고치니 달라진 행동

| 고친 것 | 고치기 전 행동 | 고친 뒤 행동 |
|---|---|---|
| 강의차시를 별도 트랙으로 | "제외됨 11건" 숫자만 보여줌 | **내일 볼 동영상 9개**를 이름까지 내놓음 |
| 두 파일 대조 절차 | 어긋난 채로 진행 | 3회차가 대조하고 **"일치. 통과"** 를 먼저 보고 |
| 강의의 마감을 닫히는 날로 | 볼 수 있는 강의를 "지남" 이라 함 | **"10월 6일까지 · D-12"** 로 정확히 |

가장 크게 배운 것은 이것이다.

> **규칙을 적어두는 것만으로는 안 지켜진다.**
> 설계할 때 "한쪽만 고치면 다음 실행이 어긋난다"고 스킬에 적어놓고도,
> 정작 1회차에서 내가 그걸 어겼다. **절차로 만들어야 지켜졌다** —
> "매번 두 파일을 대조한다" 를 실행 순서 안에 넣은 뒤에야 실제로 잡혔다.

## 다른 사람이 쓰려면 바꿔야 할 것

**고치기 쉬운 것** — 파일 몇 줄이다.

- `과목.md` 의 수강 과목과 시간표
- 제외 과목(`신입생세미나` — 내가 멘토라 제출물이 없다). 남에게는 이 규칙이 틀린다
- 화면의 과목별 색, 자동수집 사각지대 목록

**구조를 바꿔야 하는 것** — 여기서 성격이 달라진다.

- **분류 규칙이 학교에 묶여 있다.** "날짜만 있으면 과제, 시각이 있으면 강의",
  "과목 칸이 32자리 16진수면 비교과" 는 **이캠퍼스(Moodle 계열) 를 보고 세운 규칙**이다.
  다른 LMS 면 전부 다시 만들어야 한다.
- **달력 URL을 대신 보관하는 순간 개인정보 처리자가 된다.** 그 URL 하나로 그 사람
  일정 전체가 읽힌다. 암호화 보관·유출 대응·탈퇴 시 삭제가 의무가 된다.
- 지금 잠금은 토큰 하나다. 여러 명이 쓰려면 계정 체계가 필요하다.

**진짜 장벽은 기술이 아니다.** 혼자 쓰는 것은 주말에 할 수 있는 분량이지만,
남의 자격증명을 맡는 순간 책임의 문제가 된다. 그리고 그건 코드로 못 푼다.
