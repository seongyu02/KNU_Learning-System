# 2주차 과제 — 나만의 자비스

강남대학교 네트워크서비스 · 2026년 2학기 · 인공지능융합공학부

---

## 1. 해결할 문제

나는 7과목을 듣는데 과제 마감이 이캠퍼스 강의실마다 흩어져 있어서, 어느 과목에 뭐가
남았는지 한눈에 못 보고 마감이 임박해서야 알아차린다.
목록을 만들어 봐도 하루에 못 할 분량을 적어놔서 결국 아무것도 시작하지 못하고,
어제 하다 만 것은 다음 날 잊는다.

**만들 것**: 마감을 자동으로 모아 오늘 할 수 있는 것만 골라주고, 그 결과를 파일로 남겨
다음 날 이어받는 개인 비서.

**성공 기준** (이 셋이 다 돼야 성공이다)

1. 새 대화를 열어도 **어제 미완료 항목이 오늘 계획에 이어받기로 표시**된다
2. 하루 가용시간을 **넘는 계획을 제안하지 않는다** (최대 3개)
3. 모르는 마감일·소요시간을 **추정해서 채우지 않고** 사람에게 묻는다

## 2. 선택한 도구

**Claude Code**. `CLAUDE.md`(프로젝트 지침)와 `.claude/skills/`(행동 매뉴얼)를 실제로 읽는
구조라서, 수업에서 배운 지침·스킬 방식을 그대로 쓸 수 있다.
외부 서비스 연동은 이캠퍼스가 공식 제공하는 **iCal 일정 내보내기** 하나만 쓴다.

**핵심 기능은 파일과 스킬만으로 완결된다.** `/오늘` 과 `내과제/` 만 있으면 돌아간다.
화면(`웹앱.html`)과 서버(Cloudflare Workers)는 **그 위에 얹은 편의 장치**이고,
없어도 시스템은 동작한다. 과제가 요구한 것은 앞의 둘이다.

## 3. 배포본을 출발점으로 삼아 무엇을 지었나

배포받은 학습 시스템은 **공부를 시키는 시스템**이다. 내가 필요한 건 **과제를 관리하는
시스템**이라, 하는 일이 겹치지 않는다. 그래서 배포본을 고치지 않고 **그 구조를 본떠
옆에 따로 지었다.** `netsvc-학습시스템/` 은 받은 그대로 두었다.

배포본에서 **가져온 것**은 구조다. 지침(`CLAUDE.md`) · 행동 매뉴얼(`.claude/skills/`) ·
기록 폴더 — 이 세 층으로 나누는 방식이 그대로 유효했다.

| 층 | 배포본 (학습) | 내가 지은 것 (과제) | 무엇이 달라졌나 |
|---|---|---|---|
| 지침 | `netsvc-학습시스템/CLAUDE.md`<br>학습 코치 역할 | `CLAUDE.md`<br>두 시스템의 경계, **추정 금지**, **자격증명 금지**, **사용자에게 물어야 하는 결정** | 마감을 다루면 "모르는 값"이 생긴다. 지어내지 않겠다는 규칙이 새로 필요했다 |
| 스킬 | `/study` `/roadmap`<br>질문하고 채점한다 | `/오늘` 가용시간을 묻고 우선순위를 계산한다<br>`/동기화` 이캠퍼스에서 받아 반영한다 | 공부는 *이해했나*를 묻고, 과제는 *오늘 끝낼 수 있나*를 묻는다 |
| 기록 | `내학습/`<br>진행상황 · 복습큐 | `내과제/`<br>과목 · 마감목록 · 실행기록 | 복습큐는 쌓이고, 마감목록은 **지워진다.** 수명이 다르다 |
| 자료 | 사람이 직접 입력 | `일정가져오기.py`<br>이캠퍼스 iCal 자동 수집·분류 | 7과목을 손으로 옮겨 적는 게 원래 문제였다 |
| 보안 | 기본 `.gitignore` | `내과제/.env.local` + `.example` 템플릿 | 달력 URL이 인증 토큰이라 커밋되면 안 된다 |

**왜 합치지 않았나** — 한 저장소 안에 두 시스템이 있으면 스킬 호출이 섞인다.
"공부하자"와 "오늘 뭐 하지"는 다른 일이고, 폴더가 갈려 있어야 사람도 AI도 안 헷갈린다.
최상단 `CLAUDE.md` 가 그 경계를 글로 박아둔다.

## 4. 핵심 파일의 역할

```
KNU_Learning-System/
├── CLAUDE.md                       프로젝트 지침. 두 시스템의 경계, 물어야 할 결정
├── .claude/skills/
│   ├── 오늘/SKILL.md               ★ 오늘 할 과제를 정한다
│   └── 동기화/SKILL.md             ★ 이캠퍼스에서 받아 반영한다
├── netsvc-학습시스템/               배포받은 학습 시스템. 손대지 않았다
├── deploy/                         Cloudflare Workers 무인 배포 (선택)
└── 내과제/
    ├── 일정가져오기.py              이캠퍼스 iCal → 과제/강의/비교과 분류 + 대시보드 생성
    ├── sync.py / sync.bat           무인 실행용 진입점 (작업 스케줄러에 등록)
    ├── 대시보드.html                한눈에 보는 로컬 화면 (생성물, .gitignore)
    ├── 웹앱.html                    웹 계기판 소스. 완료·소요시간이 서버에 저장됨
    ├── 과목.md                      참고 정보. 제외할 과목, 자동수집 사각지대
    ├── 마감목록.md                  ★ 원본 데이터. 무엇이 남았나
    ├── 실행기록/YYYY-MM-DD.md       ★ 상태 기록. 세션을 잇는 고리
    ├── .env.local                   달력 URL. .gitignore 됨 (커밋 안 됨)
    └── .env.local.example           URL 받는 방법 설명서
```

★ 표시가 이 시스템의 뼈대다.

- **`마감목록.md`** — "무엇이 남았나". 자동 수집과 수동 등록이 합쳐지는 곳
- **`실행기록/`** — "어제 뭘 하기로 했고 뭘 못 끝냈나". **이 파일이 없으면 새 대화는
  아무것도 모르는 상태로 시작한다.** 세션 간 연속성은 전적으로 여기서 나온다
- **`오늘/SKILL.md`** — "어떤 순서로 판단하나". 우선순위 계산식과, 자비스가 계산할 것 /
  사람에게 물을 것의 경계가 표로 박혀 있다
- **`동기화/SKILL.md`** — "새 마감을 어떻게 들여오나". **사람이 넣은 값을 덮지 않는
  규칙**과, 자동 수집이 못 잡는 과목을 어떻게 다루는지가 들어 있다

## 5. 실행 방법

**최초 1회 — 달력 URL 등록**

1. 이캠퍼스 로그인 → `https://ecampus.kangnam.ac.kr/calendar/export.php`
2. `모든 이벤트` + `현재와 추후 2달` 선택 → **`일정 URL 불러오기`** 클릭
3. 나온 긴 URL을 복사
4. `내과제/.env.local.example` 를 `.env.local` 로 복사하고 `ICS_URL=` 뒤에 붙여넣기

> `현재와 추후 2달`은 상대 기간이라 URL 하나로 학기 내내 쓴다.
> `범위 설정`을 고르면 날짜가 URL에 박혀서 다음 달에 죽는다.

**매일 사용**

```
/오늘 3시간
```

가용시간을 안 적으면 먼저 물어본다. 그다음 자동으로:
이캠퍼스 일정 수집 → 과제만 분류 → 어제 미완료 이어받기 → 우선순위 계산 →
가용시간 안에서 최대 3개 제안 → `실행기록/오늘날짜.md` 저장.

**한눈에 보기 — 대시보드**

```bash
python 내과제/일정가져오기.py --html
```

`내과제/대시보드.html` 이 갱신된다. 브라우저로 열면 이렇게 나뉜다.

| 구역 | 내용 |
|---|---|
| **지금 급한 것** | 오늘 마감·지난 과제. **남은 시간이 초 단위로 실시간 갱신** |
| 이번 주 | D-1 ~ D-7 |
| 나중 | D-8 이후 |
| **당장 봐야 할 강의** | 오늘·내일 열리는 동영상 강의 |
| 다가오는 강의 | 그 이후 |

이 대시보드는 **로컬 파일**이라 인터넷 없이도 열린다. 생성된 HTML에 토큰이 들어가지
않는 것은 검사로 확인했다. (같은 화면을 서버에 올린 것이 6절의 4층이다.)

**수집 결과만 텍스트로 보기**

```bash
python 내과제/일정가져오기.py
```

## 6. 계속 쓰는 방법 — 자동화 4층

손이 얼마나 가느냐로 층이 나뉜다. 위로 갈수록 편하고, 아래로 갈수록 의존이 적다.

| 층 | 어디서 | 갱신 방식 | 필요한 것 |
|---|---|---|---|
| **4. 서버 배포** | `deadline-dashboard...workers.dev` | 서버가 직접 수집. **화면의 "지금 동기화" 버튼 + 매일 아침 1회 자동** | Cloudflare 무료 계정 |
| **3. 로컬 무인** | `내과제/대시보드.html` | 작업 스케줄러가 `sync.bat` 실행 | 없음. 로그인 불필요 |
| **2. 한마디** | 어디든 | Claude Code 에게 "일정 동기화해줘" → `/동기화` 스킬이 절차를 보장 | 대화 한 번 |
| **1. 수동** | 웹앱 | 스크립트 출력을 붙여넣기 | 손 |

**4층이 최종형이다.** 브라우저는 이캠퍼스에 직접 못 붙지만(달력 URL이 인증 토큰이라
페이지에 넣으면 유출이고, 외부 도메인 요청도 막힌다) **서버는 둘 다 해당이 없다.**
토큰을 서버가 들고 서버가 받아온다. 붙여넣기 단계가 사라진다.

배포 절차는 [deploy/README.md](deploy/README.md) 에 있다.

**화면은 한 벌만 관리한다.** `내과제/웹앱.html` 이 원본이고, Claude 아티팩트와
Cloudflare Worker 가 **같은 파일**을 쓴다. Worker 가 `claude.use("db")` 와 같은 모양의
어댑터를 주입해서, 화면 코드는 자기가 어디서 도는지 모른다.
두 벌로 갈라 관리하면 한쪽만 고치는 일이 반드시 생긴다.

**3층 등록** (관리자 권한 불필요, 한 줄):

```
schtasks /create /tn "deadline-dashboard" /tr "\"C:\KNU_Learning-System\KNU_Learning-System\내과제\sync.bat\" auto" /sc daily /st 07:43
```

해제는 `schtasks /delete /tn "deadline-dashboard" /f`.

> **`sync.bat` 은 ASCII 로만 써야 한다.** cmd.exe 는 `.bat` 을 콘솔 코드페이지로 읽어서,
> 파일 안에 한글이 있으면 명령이 통째로 깨진다. 실제로 그렇게 깨졌다.
> 한글 경로 처리는 `sync.py` 가 맡는다. 이 분리가 이 파일 두 개가 존재하는 이유다.

## 7. 알고 쓰는 한계

- **디지털트윈프로그래밍·단기 K-프로젝트I 은 이캠퍼스에 일정이 안 올라온다.**
  자동 수집으로 못 메우는 구멍이라 주 1회 수동 확인이 필요하다. `과목.md` 에 적어두었다.
- 이캠퍼스는 SSO 로그인이라 `login/token.php` 로 API 토큰을 못 받는다
  (`redirecterrordetected`). 그래서 과제 **설명 본문**은 자동으로 못 읽고 마감일만 온다.
- 강의 차시가 과제처럼 섞여 오기 때문에 분류 규칙이 필요하다.
  실제 피드 18건 중 진짜 과제는 4건이었다.
