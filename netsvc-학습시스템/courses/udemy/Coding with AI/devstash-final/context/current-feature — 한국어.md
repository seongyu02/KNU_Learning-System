# 현재 기능(Current Feature)

> 원문 [current-feature.md](current-feature.md)의 한국어 번역본입니다.

## 상태(Status)

시작 안 함(Not Started)

## 목표(Goals)

<!-- Define goals here -->

## 노트(Notes)

<!-- Additional context here -->

## 히스토리(History)

- **Initial Setup** - Next.js 16, Tailwind CSS v4, TypeScript 설정 완료 (완료)
- **Dashboard UI Phase 1** - ShadCN UI 초기화, /dashboard 경로의 대시보드 라우트, 다크 모드가 적용된 메인 레이아웃, 검색과 버튼이 있는 상단 바(top bar), 사이드바 및 메인 플레이스홀더(placeholder) (완료)
- **Dashboard UI Phase 2** - 아이템 타입 내비게이션이 있는 접이식 사이드바(collapsible sidebar), 즐겨찾기 및 최근 컬렉션, 사용자 아바타 영역, 모바일 드로어(drawer), 반응형 동작(responsive behavior) (완료)
- **Dashboard UI Phase 3** - 통계 카드(stats cards), 컬렉션 섹션, 고정된 아이템(pinned items), 최근 아이템 목록이 있는 메인 콘텐츠 영역 (완료)
- **Prisma + Neon PostgreSQL** - Neon PostgreSQL을 사용하는 Prisma 7 ORM, User·NextAuth·Item·ItemType·Collection·Tag 모델을 포함한 전체 스키마, 인덱스(index), 캐스케이드 삭제(cascade delete), 시스템 아이템 타입용 시드 파일(seed file), 초기 마이그레이션(migration) (완료)
- **Seed Data** - 데모 사용자(demo@devstash.io), 5개 컬렉션(React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources), bcryptjs 비밀번호 해싱(password hashing)이 적용된 17개 아이템(snippets, prompts, commands, links) (완료)
- **Dashboard Collections** - 컬렉션 섹션의 실제 데이터베이스 데이터, 가장 많이 사용된 아이템 타입에서 동적으로 정해지는 테두리 색상, 타입 아이콘 표시, Prisma를 사용한 서버 컴포넌트(server component) 데이터 페칭 (완료)
- **Dashboard Items** - 고정 및 최근 아이템의 실제 데이터베이스 데이터, 아이템 타입 아이콘/색상, 태그 표시, Prisma를 사용한 서버 컴포넌트 데이터 페칭 (완료)
- **Stats & Sidebar** - 통계 카드와 사이드바의 실제 데이터베이스 데이터, 개수와 커스텀 정렬이 있는 아이템 타입, 색상 표시가 있는 즐겨찾기/최근 컬렉션, "View all collections" 링크 (완료)
- **Pro Badge Sidebar** - ShadCN Badge 컴포넌트를 사용해 데스크톱 및 모바일 사이드바의 Files·Images 아이템 타입에 PRO 배지 표시 (완료)
- **Code Quality Quick Wins** - Prisma `_count`와 `take`를 사용한 N+1 쿼리 수정, 자주 쓰는 쿼리를 위한 데이터베이스 인덱스, 폴백(fallback)이 있는 공유 ICON_MAP, 공유 날짜 유틸리티, 대시보드 로딩/에러 상태, 쿼리 limit 검증 (완료)
- **Auth Setup Phase 1** - GitHub OAuth를 사용하는 NextAuth v5, 엣지 호환성(edge compatibility)을 위한 인증 설정 분리, JWT 전략을 사용하는 Prisma 어댑터(adapter), 프록시(proxy)를 통한 /dashboard 라우트 보호, user.id를 포함한 Session 타입 (완료)
- **Auth Setup Phase 2** - 이메일/비밀번호를 사용하는 Credentials 프로바이더(provider), bcrypt 검증, 검증 로직이 있는 /api/auth/register 엔드포인트 (완료)
- **Auth Setup Phase 3** - 커스텀 로그인 및 가입 페이지, 이미지/이니셜 폴백이 있는 재사용 가능한 UserAvatar 컴포넌트, 프로필 링크와 로그아웃이 있는 사이드바 사용자 드롭다운, Sonner 토스트 알림(toast notification), 인증된 세션을 사용하는 대시보드 (완료)
- **Email Verification** - Resend SDK 연동, 가입 시 검증 토큰(verification token), 검증 이메일, /api/auth/verify 엔드포인트, /verify-email 페이지, 미검증 사용자의 로그인 차단, 재발송 기능, 엣지 케이스 처리 (완료)
- **Email Verification Toggle** - 개발 중 이메일 검증을 우회하는 SKIP_EMAIL_VERIFICATION 환경 변수, 가입 시 자동 검증, 활성화 시 로그인 확인 건너뛰기 (완료)
- **Forgot Password** - 로그인 화면의 비밀번호 찾기 링크, /forgot-password 및 /reset-password 페이지, 토큰 생성과 비밀번호 재설정을 위한 API 엔드포인트, Resend를 통한 비밀번호 재설정 이메일, 접두사(prefix)를 붙여 VerificationToken 모델 재사용, 1시간 토큰 만료, 엣지 케이스 처리 (완료)
- **Profile Page** - 사용자 정보가 있는 /profile 프로필 페이지, 아이템 타입별 분류가 있는 사용량 통계, 이메일 사용자용 비밀번호 변경, 확인 다이얼로그(confirmation dialog)가 있는 계정 삭제, 비밀번호 변경 및 계정 삭제 API 엔드포인트 (완료)
- **Rate Limiting for Auth** - 인증 엔드포인트에 대한 Upstash Redis 요청 제한(rate limiting), 슬라이딩 윈도우 알고리즘(sliding window algorithm)이 적용된 재사용 가능한 rate-limit 유틸리티, 설정 가능한 한도로 login/register/forgot-password/reset-password/resend-verification 보호, Retry-After 헤더가 있는 429 응답, 페일 오픈(fail-open) 설계 (완료)
- **Items List View** - 타입별로 필터링된 아이템 목록을 위한 동적 라우트 /items/[type], 고정 우선 정렬(pinned-first sorting)이 적용된 getItemsByType 쿼리, 기존 ItemCard를 사용한 반응형 2열 그리드, 404가 있는 타입 검증, 빈 상태(empty state) (완료)
- **Vitest Setup** - 서버 액션과 유틸리티(컴포넌트 제외)의 단위 테스트를 위한 Vitest, 소스와 같은 위치에 배치된 테스트 파일, 샘플 날짜 유틸리티 테스트, 워크플로 및 코딩 표준 문서 업데이트 (완료)
- **Items List Three-Column Layout** - lg 브레이크포인트(breakpoint)에서 아이템 그리드를 2열에서 3열로 변경, 반응형 1/2/3열 레이아웃 (완료)
- **Item Drawer** - shadcn Sheet를 사용한 우측 슬라이드인 드로어, ItemCard 클릭 시 열림, 인증이 적용된 /api/items/[id]를 통해 전체 아이템 상세 페칭, 타입 아이콘/배지 표시, 액션 바(즐겨찾기/고정/복사/편집/삭제), 줄 번호가 있는 콘텐츠, 태그, 컬렉션, 날짜, 로딩 스켈레톤(skeleton), DashboardLayout의 ItemDrawerProvider 컨텍스트, 소유권 확인(ownership check)이 있는 getItemById 쿼리, 단위 테스트 (완료)
- **Item Drawer Edit Mode** - Edit 버튼을 통한 인라인 편집 모드, 저장/취소 액션 바, 모든 타입에 대해 편집 가능한 Title/Description/Tags, 텍스트 타입의 Content/Language, 링크의 URL, Zod 검증이 있는 updateItem 서버 액션, 태그 disconnect/connect-or-create가 있는 updateItem 쿼리, 토스트 알림, 저장 후 router.refresh() (완료)
- **Item Delete** - 아이템 드로어의 휴지통 버튼을 통한 삭제 기능, shadcn AlertDialog 확인이 있는 DeleteItemDialog, Zod 검증이 있는 deleteItem 서버 액션, 소유권 확인이 있는 deleteItem 쿼리, 성공 시 토스트, 드로어 닫기 및 페이지 새로고침, 단위 테스트 (완료)
- **Code Editor** - macOS 창 스타일(빨강/노랑/초록 점)이 있는 스니펫/명령어용 Monaco Editor 컴포넌트, 복사 버튼, 언어 라벨, 읽기 전용/편집 모드, 유동 높이(최대 400px), ItemDrawer 및 NewItemDialog에 통합, 아이템 목록 페이지에 타입이 미리 선택되는 타입별 "New {Type}" 버튼 (완료)
- **GitHub OAuth Redirect Fix** - 클라이언트 사이드 signIn에서 서버 사이드 Server Action 패턴으로 전환, callbackUrl 대신 redirectTo를 사용하는 src/actions/auth.ts의 signInWithGitHub 액션, 안정적인 리다이렉트를 위한 로그인 페이지의 form action (완료)
- **Item Create** - 상단 바에서 여는 shadcn Dialog 기반 새 아이템 모달, 타입 선택기(snippet/prompt/command/note/link), 타입에 따른 동적 필드, Zod 검증이 있는 createItem 서버 액션, 태그 connectOrCreate가 있는 createItem 쿼리, 단위 테스트 (완료)
- **Markdown Editor** - Write/Preview 탭이 있는 MarkdownEditor 컴포넌트, GitHub Flavored Markdown을 위한 remark-gfm을 사용하는 react-markdown, prose 스타일링이 적용된 @tailwindcss/typography 플러그인, notes/prompts를 위해 NewItemDialog 및 ItemDrawer에 통합, 읽기 전용 모드의 macOS 창 스타일, 커스텀 스크롤바 스타일링 (완료)
- **File & Image Upload** - 업로드/삭제 유틸리티가 있는 Cloudflare R2 연동, /api/upload 및 /api/download 라우트, 드래그 앤 드롭과 진행률 표시기가 있는 FileUpload 컴포넌트, PRO 배지가 있는 NewItemDialog file/image 타입, ItemDrawer 이미지 미리보기와 다운로드 버튼이 있는 파일 정보, 아이템 삭제 시 R2 파일 정리(cleanup), R2 유틸리티에 대한 25개 단위 테스트 (완료)
- **Image Gallery View** - aspect-video를 사용한 16:9 비율 썸네일이 있는 ImageThumbnailCard 컴포넌트, 이미지를 채우는 object-cover, 300ms 전환이 적용된 5% 확대 호버 줌 효과, ItemWithType 인터페이스에 fileUrl 추가, 아이템 페이지에서 image 타입에만 조건부 렌더링 (완료)
- **File List View** - 파일 확장자 아이콘이 있는 FileListRow 컴포넌트, /items/files를 위한 단일 열 목록 레이아웃(Google Drive 스타일), 각 행에 파일 아이콘/제목/이름/크기/날짜/다운로드 버튼 표시, 행 호버 하이라이트, 클릭 시 ItemDrawer 열림, /api/download를 통한 다운로드, 모바일 반응형 스태킹(stacking), fileName/fileSize로 확장된 ItemWithType (완료)
- **Quick Copy Button** - 호버 시 나타나는 아이템 카드의 복사 아이콘, 텍스트 아이템은 콘텐츠를, 링크는 URL을 복사, 초록색 체크마크 피드백 (완료)
- **Security & Performance Audit Fixes** - Bcrypt 라운드를 12로 표준화, 디바운스(debounce)된 마크다운 에디터 리사이즈, URL 프로토콜 검증(http/https만 허용), 원격 패턴(remote patterns) 설정이 적용된 R2 이미지용 next/image, 업로드 요청 제한(사용자당 시간당 10회) (완료)
- **Code Deduplication Refactor** - 3개 컴포넌트용 useClipboard 훅, items.ts의 toItemWithType/toItemDetail 변환, validation.ts의 parseZodErrors/safeUrlSchema, EditorHeader 공유 컴포넌트, collections.ts의 countItemTypes/getDominantColor, formatLongDate 유틸리티(약 255줄 제거) (완료)
- **Collection Create** - 상단 바의 New Collection 버튼, 이름/설명 필드가 있는 NewCollectionDialog 모달, Zod 검증이 있는 createCollection 서버 액션, createCollection DB 쿼리, 8개 단위 테스트, 토스트 알림, UI 업데이트를 위한 router.refresh() (완료)
- **Item Collections Assignment** - shadcn Popover + Command를 사용한 다중 선택 CollectionPicker 컴포넌트, getUserCollections 쿼리와 서버 액션, 컬렉션 연결이 있는 createItem/updateItem, NewItemDialog 및 ItemDrawer 편집 모드에 통합, 신규 테스트 6개 (완료)
- **Collections Pages** - 모든 컬렉션을 나열하는 /collections 페이지, 타입별로 그룹화된 아이템을 보여주는 /collections/[id] 상세 페이지, getUserById 유틸리티, getAllCollections/getCollectionById/getItemsByCollection 쿼리, 상세 페이지로 연결되는 컬렉션 카드, 5개 단위 테스트 (완료)
- **Collection Management Actions** - /collections/[id] 페이지의 편집/삭제/즐겨찾기 버튼, 컬렉션 카드의 3점 드롭다운, EditCollectionDialog 및 DeleteCollectionDialog 모달, updateCollection/deleteCollection 서버 액션 및 DB 쿼리, 컬렉션 삭제 시 아이템은 삭제되지 않음, 신규 단위 테스트 17개 (완료)
- **Global Search / Command Palette** - Cmd+K/Ctrl+K로 커맨드 팔레트(command palette) 열기, 아이템과 컬렉션 전반 검색, 타입 아이콘과 아이템 개수가 있는 그룹화된 결과, 키보드 내비게이션, TopBar 검색 트리거, 데이터 사전 페칭(pre-fetching)이 있는 SearchProvider 컨텍스트, 더 엄격한 부분 문자열 매칭(substring matching), 신규 단위 테스트 7개 (완료)
- **Pagination** - 번호가 매겨진 페이지와 이전/다음 버튼이 있는 재사용 가능한 Pagination 컴포넌트, /items/[type]·/collections·/collections/[id] 페이지의 페이지네이션, skip/take를 사용한 서버 사이드 페이지네이션, 페이지네이션 상수(ITEMS_PER_PAGE=21, COLLECTIONS_PER_PAGE=21), 대시보드 한도(DASHBOARD_COLLECTIONS_LIMIT=6, DASHBOARD_RECENT_ITEMS_LIMIT=10) (완료)
- **Favorites Sorting** - /favorites 페이지의 클라이언트 사이드 정렬, 이름/날짜/타입 기준 정렬이 있는 FavoritesItemList 컴포넌트, 이름/날짜 기준 정렬이 있는 FavoritesCollectionList 컴포넌트, 컴팩트한 모노스페이스(monospace) 스타일의 shadcn Select 드롭다운, 정렬된 목록을 위한 useMemo, 기본값은 최신순 정렬 (완료)
- **Pinned Items** - 소유권 확인과 Zod 검증이 있는 toggleItemPin DB 쿼리 및 서버 액션, 낙관적 상태 업데이트(optimistic state update)로 연결된 ItemDrawer 고정 버튼, 고정 시 파란색 하이라이트, 토스트 피드백, router.refresh(), 신규 단위 테스트 5개 (완료)
- **Favorite Toggle** - 소유권 확인과 Zod 검증이 있는 toggleItemFavorite 및 toggleCollectionFavorite DB 쿼리 및 서버 액션, 낙관적 상태 업데이트로 연결된 ItemDrawer 즐겨찾기 버튼, CollectionCard 드롭다운의 Favorite/Unfavorite 메뉴 항목, 컬렉션 상세 페이지의 CollectionActions 별표 버튼, 토스트 피드백, router.refresh(), 신규 단위 테스트 10개 (완료)
- **Favorites Page** - /favorites로 연결되는 TopBar의 별표 아이콘, getFavoriteItems 및 getFavoriteCollections DB 쿼리, 컴팩트한 VS Code/터미널 스타일 행이 있는 FavoriteItemRow 및 FavoriteCollectionRow 컴포넌트, 아이템/컬렉션 섹션이 분리된 /favorites 페이지, 아이템 클릭 시 ItemDrawer 열림, 컬렉션 클릭 시 상세로 이동, 빈 상태 (완료)
- **Settings Page** - 비밀번호 변경과 계정 삭제가 있는 /settings 설정 페이지, 사이드바 드롭다운의 Settings 링크, getUserWithSettings 유틸리티, 간소화된 ProfileInfo (완료)
- **Editor Preferences Settings** - 설정 페이지의 에디터 환경설정(폰트 크기, 탭 크기, 워드 랩(word wrap), 미니맵(minimap), 테마 드롭다운/토글), 마이그레이션이 적용된 User 모델의 editorPreferences JSON 컬럼, EditorPreferencesProvider 컨텍스트, updateEditorPreferences 서버 액션, 사용자 환경설정을 적용하는 Monaco 에디터, 토스트가 있는 자동 저장, 19개 단위 테스트 (완료)
- **Homepage** - 정적 HTML 프로토타입을 11개의 Next.js 컴포넌트(Navbar, HeroSection, ChaosAnimation, DashboardPreview, FeaturesSection, AISection, PricingSection, CTASection, Footer, ScrollFadeIn)로 변환, src/app/page.tsx의 페이지 셸, rAF/마우스 반발(mouse repulsion)을 사용한 혼돈-질서(chaos-to-order) 애니메이션, IntersectionObserver를 통한 스크롤 페이드인, 가격 월간/연간 토글, 모바일 햄버거 메뉴가 있는 내비바 스크롤 효과, 반응형 3->2->1열 그리드, 파란 그라디언트 강조가 있는 다크 테마, 부드러운 스크롤, /sign-in·/register 및 앵커 섹션으로 연결된 모든 링크 (완료)
- **Homepage Mockup** - prototypes/homepage/의 다크 테마 정적 마케팅 홈페이지 프로토타입, 혼돈-질서 애니메이션을 특징으로 하는 히어로 섹션(requestAnimationFrame으로 움직이는 8개의 떠다니는 아이콘, 마우스 반발, 맥동하는 화살표, 상단 바와 콘텐츠 카드가 있는 대시보드 미리보기), 스크롤 불투명도가 있는 고정 내비바, 강조 색상이 있는 6개의 기능 카드, Pro 배지가 있는 AI 섹션과 구문 강조(syntax highlighting) 및 AI 생성 태그 데모가 있는 코드 에디터 목업(mockup), Free/Pro 카드와 월간/연간 토글이 있는 가격 섹션, CTA 섹션, 동적 연도가 있는 푸터, IntersectionObserver를 통한 스크롤 페이드인 애니메이션, 완전 반응형 모바일 레이아웃 (완료)
- **Top Bar Mobile Responsive** - 모바일에서의 상단 바 오버플로 수정(320-375px 뷰포트에 590px 콘텐츠), sm 미만에서 DS 배지는 유지하고 "DevStash" 로고 텍스트 숨김, 모바일에서 검색 바를 아이콘 전용으로 축소하되 여전히 커맨드 팔레트 열기, 모바일에서 New Item/New Collection 버튼을 단일 + 아이콘 DropdownMenu로 대체, 작은 화면을 위한 간격/패딩 축소, 고정 요소의 shrink-0, sm+ 브레이크포인트에서 전체 레이아웃 유지 (완료)
- **Auth Nav & Dashboard Logo** - 공유 (auth)/layout.tsx를 통해 모든 인증 페이지(sign-in, register, verify-email, forgot-password, reset-password)에 홈페이지 Navbar 추가, 대시보드 TopBar의 DS 파란 박스를 홈페이지 내비와 일치하는 FolderOpen 아이콘으로 교체, Navbar 앵커 링크를 절대 경로(/#features, /#pricing)로 수정, 내비바 오프셋을 위한 인증 페이지 최소 높이 조정 (완료)
- **Stripe Phase 1 (Core Infrastructure)** - src/lib/stripe.ts의 Stripe SDK 초기화, src/lib/usage.ts의 getUserUsage/canCreateItem/canCreateCollection가 있는 사용량 한도 유틸리티(무료 티어의 MAX_ITEMS=50, MAX_COLLECTIONS=3), 9개 단위 테스트, 인증 콜백의 DB 동기화와 함께 NextAuth 세션/JWT 타입에 isPro 추가, 월간/연간 plan-to-priceId 매핑 및 Stripe 고객 찾기/생성이 있는 결제(checkout) 세션 API 라우트(POST /api/stripe/checkout), 고객 포털 API 라우트(POST /api/stripe/portal), Stripe 환경 변수가 있는 .env.example (완료)
- **Stripe Phase 2 (Webhooks, Feature Gating & UI)** - checkout.session.completed·invoice.paid·invoice.payment_failed·customer.subscription.updated·customer.subscription.deleted에 대한 /api/webhooks/stripe의 Stripe 웹훅 핸들러(멱등성(idempotent) updateMany 및 typeof 확인), createItem(file/image의 Pro 타입 확인 + 50개 아이템 한도)·createCollection(3개 컬렉션 한도)·업로드 라우트(403이 있는 Pro DB 확인)의 기능 게이팅(feature gating), 플랜 배지·사용량 개수·업그레이드 버튼($8/월, $72/년)·결제 포털 관리가 있는 BillingSettings 컴포넌트, useSearchParams를 통한 업그레이드 성공 토스트, 설정 페이지의 에디터와 계정 섹션 사이에 연결, 신규 단위 테스트 4개 (완료)
- **AI Auto-Tagging** - OpenAI 클라이언트 유틸리티(Responses API를 통한 gpt-5-nano), 인증/Pro 게이팅/Zod 검증/요청 제한(시간당 20회)이 있는 generateAutoTags 서버 액션, 수락/거부 배지 컨트롤이 있는 SuggestTagsButton 컴포넌트, NewItemDialog 및 ItemDrawer 편집 모드에 통합(Pro 전용), DashboardLayout/TopBar/ItemsPageHeader/ItemDrawerProvider를 통해 전달되는 isPro prop, 콘텐츠를 2000자로 축약, {tags:[...]}와 [...] 응답 형식 모두 처리, 소문자로 정규화하며 중복 제거(dedup), 13개 단위 테스트 (완료)
- **AI Description Generator** - 인증/Pro 게이팅/Zod 검증/요청 제한이 있는 generateDescription 서버 액션, "Describe" 라벨과 Sparkles 아이콘이 있는 GenerateDescriptionButton 컴포넌트, NewItemDialog 및 ItemDrawer 편집 모드의 Description 라벨 옆에 통합(Pro 전용), 2000자 축약과 함께 title/content/URL/language/type 컨텍스트 사용, {description:"..."}와 일반 문자열 AI 응답 형식 처리, 신규 단위 테스트 12개 (완료)
- **AI Explain Code** - 인증/Pro 게이팅/Zod 검증/요청 제한이 있는 explainCode 서버 액션, 아이템 드로어 읽기 모드에서 snippet/command 타입의 코드 에디터 헤더에 Explain 버튼(Sparkles 아이콘), 생성 후 Code/Explain 탭 인터페이스, 같은 컨테이너에 마크다운으로 렌더링된 설명, 무료 사용자를 위한 Crown 아이콘 + 툴팁(Pro 게이팅 UI), 생성 중 Loader2 스피너, 에러 토스트, 대시보드 레이아웃의 TooltipProvider, EditorHeader의 extraButtons prop, 신규 단위 테스트 12개 (완료)
- **AI Prompt Optimizer** - 인증/Pro 게이팅/Zod 검증/요청 제한이 있는 optimizePrompt 서버 액션, 아이템 드로어 읽기 모드에서 prompt 타입의 MarkdownEditor 헤더에 Optimize 버튼(Sparkles 아이콘), 생성 후 Original/Optimized 탭 인터페이스, updateItem을 통해 최적화된 프롬프트를 수락하고 저장하는 "Use This" 버튼(Check 아이콘, 에메랄드 그린), 무료 사용자를 위한 Crown 아이콘 + 툴팁(Pro 게이팅 UI), MarkdownEditor의 EditorHeader에 추가된 extraButtons prop, 신규 단위 테스트 12개 (완료)
- **Drawer & Editor Font Sizes** - 아이템 드로어와 에디터의 가독성 개선: 설명 텍스트와 URL 링크를 text-sm(14px)에서 text-base(16px)로 상향, 마크다운 에디터 미리보기에서 prose-sm 제거로 더 나은 line-height와 함께 기본 16px 적용, 마크다운 textarea를 text-base로 상향, editor-header.tsx·code-editor.tsx·markdown-editor.tsx 전반에서 에디터 툴바 버튼(Copy, Explain, Optimize, 언어 라벨, 탭)을 text-xs(12px)에서 text-sm(14px)로 상향 (완료)
- **UI Polish Fixes** - 데스크톱 및 모바일 사이드바에서 usePathname()을 통한 사이드바 활성 링크 하이라이팅(타입 링크, 컬렉션 링크, "View all collections"), 가입 페이지의 GitHub OAuth 버튼(구분선과 space-y-4 간격), 홈페이지 가격 Pro 기능에 "AI Prompt Optimizer" 추가, 아이템 카드의 pb-10을 통한 복사 버튼 겹침 수정 (완료)
- **Action Utils Refactor** - src/lib/action-utils.ts로 공유 액션 헬퍼 추출: 12개 이상의 인증 확인 블록을 대체하는 getAuthedSession(), 4개의 Pro 게이팅 블록을 대체하는 requirePro(), 4개의 요청 제한 블록을 대체하는 checkAiRateLimit(), 공유 ActionResult<T> 인터페이스; validation.ts에 5개의 일회성 Zod ID 스키마를 대체하는 validateId() 추가; 5개 액션 파일(items, collections, ai, settings, search) 전체 리팩터링, 약 168줄 순감소, 198개 테스트 통과 (완료)
- **Component Refactor** - 16개 파일에 걸쳐 9개의 공유 컴포넌트/유틸리티 추출: ConfirmDeleteDialog, DialogFormFooter, ProAiButton, FormError, GitHubAuthSection, SortableSection(공유 컴포넌트), SidebarNav, UserMenu(레이아웃 컴포넌트), 가격 상수와 startCheckout 유틸리티; PRO_FEATURES 불일치 버그 수정; 약 450줄 순감소, 198개 테스트 통과 (완료)
- **Language Selector Dropdown** - NewItemDialog 및 ItemDrawer 편집 모드에서 자유 텍스트 언어 입력을 Select 드롭다운(31개 언어)으로 대체, 사용자가 입력하는 즉시 구문 강조가 되도록 언어 선택기를 콘텐츠 에디터 위로 이동, editor.ts의 LANGUAGES 상수 (완료)
- **Import/Export** - archiver를 사용한 /api/export 라우트를 통한 JSON 내보내기(무료)와 파일 포함 ZIP(Pro), Zod 검증/Prisma 트랜잭션(transaction)/중복 감지/무료 티어 한도/file-image 필터링이 있는 importData 서버 액션, 설정 페이지의 Export JSON/Export ZIP(Pro)/Import 버튼이 있는 DataSettings 컴포넌트, 파일 드롭 존과 미리보기 요약이 있는 ImportDialog, getUserExportData DB 쿼리, previewImport 및 importData 액션, 가져오기 시 Pro 사용자의 파일 참조 보존, 신규 단위 테스트 17개(총 215개) (완료)
- **Vercel Analytics** - @vercel/analytics 패키지 설치, 모든 페이지의 페이지 뷰 추적을 위해 루트 레이아웃에 Analytics 컴포넌트 추가 (완료)
