# Container Lifecycle

## 개요
- 첫 Docker Create 다음은 Docker 시작이죠 이 짐작하시듯 Docker Create Create Create Create는 이미지의 컨테이너를 생성하는 데 사용되고 start는 이미지를 시작하는 데 사용되죠

## 내용
### 자막·본문 기반 핵심 내용
- 첫 Docker Create 다음은 Docker 시작이죠 이 짐작하시듯 Docker Create Create Create Create는 이미지의 컨테이너를 생성하는 데 사용되고 start는 이미지를 시작하는 데 사용되죠
- Docker 런, Docker Start Docker 생성은 이런 차이가 있죠 여기서 잠시 멈출게요
- 지난 섹션에서 Docker PS Command를 실행했는데 한 가지 변형을 봤죠 Docker, PS Dash 대시보드를 켰을 때 우리 기계를 작동시킨 모든 컨테이너의 프린트가 나왔어요 컨테이너가 언제 문을 닫는지 왜 폐쇄하는 거죠?
- 컨테이너를 만드는 것과 그걸 시작하는 건 사실 두 개의 분리된 프로세스죠 Docker 실행에 추가로 두 가지 명령이 있어요 새 컨테이너 시작에 사용할 Docker를 명령줄에서 실행하는 건 다른 명령 2개를 함께 실행하는 것과 동일하죠

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `Docker`, `Docker PS Command`, `Docker Create`, `Docker Create Create Create Create`, `Docker Start`, `Docker Run`, `Docker Start Docker`

## 예시
`Docker`, `Docker PS Command`, `Docker Create`, `Docker Create Create Create Create`, `Docker Start`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Container Lifecycle**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19223674#overview)
