# Tagging an Image

## 개요
- 엔터를 치면 나오네요 리디스의 복사본이 나왔어요 무시할 수 있는 경고죠 여기서 짚고 넘어가고 싶은 한 가지는 Docker 실행을 실행해 해당 ID를 붙여넣는 겁니다 여하튼 해당 ID를 붙여넣는 게 좀 성가셔요 지난 과정에선 훨씬 나았죠 그냥 Docker Run, Redis 또는 Docker Run Hello World 그런 경우 ID를 외울 필요가 없었죠 Docker 빌드 명령 형태를 약간 변경함으로써 동일한 작업을 할 수 있었어요

## 내용
### 자막·본문 기반 핵심 내용
- 엔터를 치면 나오네요 리디스의 복사본이 나왔어요 무시할 수 있는 경고죠 여기서 짚고 넘어가고 싶은 한 가지는 Docker 실행을 실행해 해당 ID를 붙여넣는 겁니다 여하튼 해당 ID를 붙여넣는 게 좀 성가셔요 지난 과정에선 훨씬 나았죠 그냥 Docker Run, Redis 또는 Docker Run Hello World 그런 경우 ID를 외울 필요가 없었죠 Docker 빌드 명령 형태를 약간 변경함으로써 동일한 작업을 할 수 있었어요
- 이 시점에서 우린 Docker 파일에서 새 사용자 지정 이미지를 만들 능력이 있습니다 Docker를 실행해 그렇게 할 수 있죠
- 이 이미지로부터 컨테이너를 시작할 수 있습니다 Docker Run을 실행해서요 그런 다음 우리가 제공한 이름을 사용하죠 스티븐 그리더나 여러분의 태그가 뭐든 슬래시 Redis요 그럼 실제로 마지막에 버전을 지정할 필요가 없어요
- 만의 이미지를 만들 땐 언제나 Docker ID 접두사를 붙이세요 다시 터미널로 돌아가 Docker를 실행할게요 t를 빌드하세요

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `Docker`, `Redis`, `Docker Run`, `Docker Run Hello World`, `Docker Build`, `Docker ID a/`, `Docker ID`, `docker ID/ redis`

## 예시
`Docker`, `Redis`, `Docker Run`, `Docker Run Hello World`, `Docker Build`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Tagging an Image**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19223730#overview)
