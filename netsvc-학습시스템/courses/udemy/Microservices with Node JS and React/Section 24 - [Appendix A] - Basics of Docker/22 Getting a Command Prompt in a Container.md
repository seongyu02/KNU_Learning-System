# Getting a Command Prompt in a Container

## 개요
- 컨테이너 내부에서 명령을 실행하는 거죠 Docker EXEC, Docker EXEC을 온종일 반복할 필요 없이요

## 내용
### 자막·본문 기반 핵심 내용
- 컨테이너 내부에서 명령을 실행하는 거죠 Docker EXEC, Docker EXEC을 온종일 반복할 필요 없이요
- 이런 것도 할 수 있죠 docker cli를 직접 실행한다든가 하는 식으로요 docker c c cli 말고 cli를 읽으면 시작되죠 빨간색은 cli예요
- Docker PS를 실행해 컨테이너 ID를 확보할게요 Docker Exec을 실행할 거예요
- 변수를 내보낼 수 있어요 B=5 같은 환경 변수요 그런 다음 에코를 입력하죠 따라서 S-H로 Docker EXEC 명령어를 사용하면 컨테이너 컨텍스트 안에서 단말기 액세스가 가능해집니다 디버깅에 아주 강력한 기능이죠

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `Docker`, `Docker EXEC`, `Docker

EXEC`, `Docker PS`, `docker cli`, `docker c c cli`, `Git Bash`

## 예시
`Docker`, `Docker EXEC`, `Docker

EXEC`, `Docker PS`, `docker cli`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Getting a Command Prompt in a Container**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19223702#overview)
