# Our First Test

## 개요
- 새로운 JSON 웹 토큰을 만들려고 해요 JSON 웹 토큰을 생성하려고 할 때 JWT_ 키의 환경 변수에 액세스하려 이전에 인덱스드 파일 안에서 정의되었는지 확인을 해왔지만 지금은 indexed와 APTs 파일을 두 개의 개별 파일로 나누어 놓았기 때문에 더 이상 해당 환경 변수가 존재하는 걸 확인하지 사실 지금은 존재하지 않아요 이 테스트 환경에서 모든 코드를 실행할 이 환경 변수는 포드 안에서 코드를 실행할 때만 정의됩니다 왜냐하면 이 환경 변수를 배포 구성 파일 내에서 정의하도록 지정했으니까요

## 내용
### 자막·본문 기반 핵심 내용
- 새로운 JSON 웹 토큰을 만들려고 해요 JSON 웹 토큰을 생성하려고 할 때 JWT_ 키의 환경 변수에 액세스하려 이전에 인덱스드 파일 안에서 정의되었는지 확인을 해왔지만 지금은 indexed와 APTs 파일을 두 개의 개별 파일로 나누어 놓았기 때문에 더 이상 해당 환경 변수가 존재하는 걸 확인하지 사실 지금은 존재하지 않아요 이 테스트 환경에서 모든 코드를 실행할 이 환경 변수는 포드 안에서 코드를 실행할 때만 정의됩니다 왜냐하면 이 환경 변수를 배포 구성 파일 내에서 정의하도록 지정했으니까요
- 처음부터 async를 추가하는 게 훨씬 쉬워요 나중에 추가하는 걸 잊거나 추가하는 걸 잊을까 봐 걱정할 필요가 없죠 오류가 날까 이 파일을 저장하고 실행하려면 터미널로 가서 npm을 실행하고 auth 디렉터리에서 테스트를 실행해요
- 어디서 요청을 할지 어떤 메서드를 쓸지 알려주는 거예요 이 경우 게시 요청을 만들어야 해요 API 사용자가 가입한 루트에 게시 요청을 슬래시 API를 하죠
- 아주 기본적이고 직설적인 테스트를 위해 작성해야 할 모든 것이죠 이 함수를 async로 표시한 게 보이실 겁니다 이 안에 깨어있는 키워드를 사용하지 습관적으로 하는 거예요

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `API`, `async`, `npm`, `JSON`

## 예시
`API`, `async`, `npm`, `JSON`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Our First Test**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19120460#overview)
