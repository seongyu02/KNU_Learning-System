# Defining the Ticket Model

## 개요
- 여기 이 형식은 TypeScript가 아니라 Mongoose가 사용해요 여기 문자열이란 건 JavaScript에서 전역 문자열 생성자를 가리키는 거죠 그 말은 대문자 S가 있어야 한다는 거예요

## 내용
### 자막·본문 기반 핵심 내용
- 여기 이 형식은 TypeScript가 아니라 Mongoose가 사용해요 여기 문자열이란 건 JavaScript에서 전역 문자열 생성자를 가리키는 거죠 그 말은 대문자 S가 있어야 한다는 거예요
- 여기 모든 인터페이스에서는 TypeScript와 관련된 한 유형을 참조합니다 이건 TypeScript 전용 유형이죠
- 마지막으로 사용자 ID요 유형은 문자열이죠 어쩔 수 없어요 mongoose 스키마에 대한 두 번째 인수로 다른 개체를 넣겠습니다 JSON 함수에 대한 커스텀 함수를 넣는 곳입니다 ID 속성을 마사지해야 하니까요
- 두 번째 인수 안에 JSON을 추가할게요 변환 함수를 정의할게요 이건 두 가지 논쟁이 필요해요 닥과 레트 렛은 JSON으로 변하기 직전의 오브젝트가 될 거예요

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `TypeScript`, `Mongoose`, `JavaScript`, `JSON`

## 예시
`TypeScript`, `Mongoose`, `JavaScript`, `JSON`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Defining the Ticket Model**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19123232#overview)
