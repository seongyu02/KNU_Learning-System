# Auth Service Setup

## 개요
- 그런 다음 이 인증 디렉토리 내에서 npm init y를 실행합니다. 패키지 도트 JSON 파일이 있습니다.

## 내용
### 자막·본문 기반 핵심 내용
- 그런 다음 이 인증 디렉토리 내에서 npm init y를 실행합니다. 패키지 도트 JSON 파일이 있습니다.
- 이 src 디렉토리에 모든 코드를 넣을 것입니다. 먼저 앱 또는 실례합니다 인덱스를 만드는 것부터 시작하겠습니다. ts. 이 인덱스에 있는 파일로 시작하려면 ts 파일을 만든 다음 기본 구문, 기본 가져오기 등 익스프레스 애플리케이션을 실행할 수 있는 모든 것을 넣을 것입니다.
- 그런 다음 JSON 미들웨어를 연결합니다. 따라서 앱닷은 JSON을 사용합니다. 아직 경로 핸들러 설정에 대해서는 걱정하지 않겠습니다.
- Kubernetes를 사용하기 시작하면 포트는 실제로 차이가 없다는 것을 기억하세요. 왜냐하면 우리는 이 모든 다른 서비스, 즉 Kubernetes 서비스를 만들기 시작할 것이기 때문입니다.

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `npm`, `JSON`, `ts`, `Kubernetes`, `npm init y`

## 예시
`npm`, `JSON`, `ts`, `Kubernetes`, `npm init y`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Auth Service Setup**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/49591749#overview)
