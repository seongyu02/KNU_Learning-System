# Dockerizing the Posts Service

## 개요
- Docker 파일을 만들어 Docker 빌드 명령을 실행하세요 이 다이어그램에서 제가 다양한 명령을 나열했습니다 Post 프로젝트를 위해 Docker 파일 안에서 쓰게 될 것들이죠

## 내용
### 자막·본문 기반 핵심 내용
- Docker 파일을 만들어 Docker 빌드 명령을 실행하세요 이 다이어그램에서 제가 다양한 명령을 나열했습니다 Post 프로젝트를 위해 Docker 파일 안에서 쓰게 될 것들이죠
- 그런 다음 이미지 및 컨테이너를 좀 다뤄볼 거예요 post service를 만들려면 거기서 이미지를 생성해야 해요 이미지를 만들려면요
- 노드 기반 이미지를 풀다운하고 작업 디렉터리를 설정하고 패키지된 JSON을 복사하고 패키지 JSON 파일을 이용한 설치 종속성을 설정하죠
- Docker 프로죠? 도커 프로가 되길 바라진 않아요 이 컨테이너가 뭔지, 이미지가 뭔지 이미지를 어떻게 생성하고 컨테이너를 어떻게 생성하는지에 대한 기본적인 이해를 갖길 바라요 그런 지식을 강화하기 위한 명령 몇 이 비디오에선 Docker를 시작해 게시 서비스를 호출합니다

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `Docker`, `service`, `json`, `NPM`, `docker build`

## 예시
`Docker`, `service`, `json`, `NPM`, `docker build`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Dockerizing the Posts Service**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19099484#overview)
