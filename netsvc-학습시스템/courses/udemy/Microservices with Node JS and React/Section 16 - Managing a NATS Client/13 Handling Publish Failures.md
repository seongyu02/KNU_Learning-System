# Handling Publish Failures

## 개요
- NATS가 다운되거나 NATS와의 연결이 말도 안 되는 이유로 다운되어도 티켓을 데이터베이스에 저장할 수 있고 데이터베이스에 트랜잭션을 저장하고 이벤트를 저장하고 바라건대 미래 시점에 NATS 연결이 백업되면 이 분리된 코드나 프로세스나 그 외 어떤 것도 이 이벤트를 끌어내 NATS에 게시할 수 있죠

## 내용
### 자막·본문 기반 핵심 내용
- NATS가 다운되거나 NATS와의 연결이 말도 안 되는 이유로 다운되어도 티켓을 데이터베이스에 저장할 수 있고 데이터베이스에 트랜잭션을 저장하고 이벤트를 저장하고 바라건대 미래 시점에 NATS 연결이 백업되면 이 분리된 코드나 프로세스나 그 외 어떤 것도 이 이벤트를 끌어내 NATS에 게시할 수 있죠
- 지난 비디오에서 우린 정말 큰 문제에 빠질 수 있다는 걸 봤죠 데이터베이스에 레코드를 성공적으로 저장했는데 방금 저장한 레코드를 묘사하는 이벤트를 방출하지 못하면요
- 다른 데이터베이스나 서비스를 업데이트하지 않아도 사용자는 우리 앱 안에서 이상한 데이터를 보게 되죠
- 이 모든 문제를 해결하기 위해서요 이벤트 버스에 이벤트를 방출하는 대신 데이터베이스에 그 이벤트를 저장하는 이 경우에는 트랜잭션이든 티켓이든 어떤 기록이든 가져옵니다 데이터베이스에 저장하고 동시에 내보내고 싶은 이벤트도 가져오는 거죠

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `NATS`

## 예시
`NATS`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Handling Publish Failures**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19485352#overview)
