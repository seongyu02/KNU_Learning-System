# Node Nats Streaming Installation

## 개요
- Node Nats Streaming Installation In the upcoming lecture, we will be importing the Node Nats Streaming module to use in our Nats Wrapper. We haven't actually installed this library to our tickets service, so, let's do that now: npm install node-nats-streaming

## 내용
### 자막·본문 기반 핵심 내용
- Node Nats Streaming Installation In the upcoming lecture, we will be importing the Node Nats Streaming module to use in our Nats Wrapper. We haven't actually installed this library to our tickets service, so, let's do that now: npm install node-nats-streaming

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `Nats`, `service`, `npm`, `Node Nats Streaming Installation`, `Node Nats Streaming module to use in our Nats Wrapper. We haven`, `npm install node-nats-streaming`

## 예시
`Nats`, `service`, `npm`, `Node Nats Streaming Installation`, `Node Nats Streaming module to use in our Nats Wrapper. We haven`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Node Nats Streaming Installation**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/33158876#overview)
