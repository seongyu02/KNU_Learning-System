# Building Post Submission

## 개요
- 이 안에서, 바로 그 다음에 Post Micro Service에 요청을 이 함수 안에서 async, await 구문을 사용할 겁니다 promise나 콜백 같은 걸 다루기보다는요

## 내용
### 자막·본문 기반 핵심 내용
- 이 안에서, 바로 그 다음에 Post Micro Service에 요청을 이 함수 안에서 async, await 구문을 사용할 겁니다 promise나 콜백 같은 걸 다루기보다는요
- post service에 post request를 할 거예요. 터미널로 빨리 돌아가면 post service를 꼭 기억하세요.
- Create post라고 나오죠 우리 모형에서 본 것처럼요 POST Create compantor도요
- 헤드 요소의 아무데나 방금 복사한 두 개의 태그를 붙여넣을 거예요 방금 복사한 태그 중 하나는 스크립트 태그입니다 Bootstrap 프로젝트와 관련된 모든 JavaScript 코드를 갖고 있죠

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `JavaScript`, `API`, `Service`, `async`, `await`, `promise`, `request`, `HTTP`

## 예시
`JavaScript`, `API`, `Service`, `async`, `await`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Building Post Submission**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19099016#overview)
