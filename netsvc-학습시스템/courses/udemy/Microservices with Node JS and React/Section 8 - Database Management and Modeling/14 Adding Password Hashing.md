# Adding Password Hashing

## 개요
- promise ify가 있어서 이 콜백 기반 함수를 promise 기반 구현으로 바꿀 수 있죠 async await와 호환되는 거예요

## 내용
### 자막·본문 기반 핵심 내용
- promise ify가 있어서 이 콜백 기반 함수를 promise 기반 구현으로 바꿀 수 있죠 async await와 호환되는 거예요
- 이렇게 쓸게요Cust script async promise ify는 이런 스크립트예요
- 온라인에서 MongoDB와 Mongoose를 사용하면서 암호 해싱을 구현하는 방법에 대한 다양한 자습서를 보시면 많은 분들이 여러분의 사용자 모델 파일에 암호 해싱 로직을 직접 놓는 것을 권장하고 있어요.
- 단점은 콜백 기반이라는 거죠 우린 최종적으로 async를 사용하길 원하죠 await for 여기서 많은 논리를 작업해요

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `MongoDB`, `Mongoose`, `async`, `await`, `promise`, `TypeScript`

## 예시
`MongoDB`, `Mongoose`, `async`, `await`, `promise`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Adding Password Hashing**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19119672#overview)
