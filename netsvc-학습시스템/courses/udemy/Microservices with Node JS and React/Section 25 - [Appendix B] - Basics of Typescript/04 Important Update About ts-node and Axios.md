# Important Update About ts-node and Axios

## 개요
- Important Update About ts-node and Axios In the upcoming lecture, we will be installing axios to use in our demo project. To use the latest versions of axios, we need to install the @types/node library: npm install --save-dev @types/node Also, Typescript 5.6 introduced some breaking changes related to imports. ts-node is currently not…

## 내용
### 자막·본문 기반 핵심 내용
- Important Update About ts-node and Axios In the upcoming lecture, we will be installing axios to use in our demo project. To use the latest versions of axios, we need to install the @types/node library: npm install --save-dev @types/node Also, Typescript 5.6 introduced some breaking changes related to imports. ts-node is currently not…
- In place of ts-node, you will need to use a different library to execute TS code throughout this course. Instead of running the command as shown in the videos: ts-node index.ts Use npx tsx instead by running the following command: npx tsx index.ts Note - This is something that you will need to do throughout the remainder of the course,…

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `ts`, `npm`, `Typescript`, `tsx`, `node and Axios`, `node library`, `npm install --save-dev`, `node

Also`, `node is currently not in sync with this since it has not seen an update since 2023`, `node index.ts`

## 예시
`ts`, `npm`, `Typescript`, `tsx`, `node and Axios`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Important Update About ts-node and Axios**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/34794764#overview)
