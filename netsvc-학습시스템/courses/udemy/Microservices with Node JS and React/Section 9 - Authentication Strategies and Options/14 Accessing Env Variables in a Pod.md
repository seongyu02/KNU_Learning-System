# Accessing Env Variables in a Pod

## 개요
- TypeScript를 만족시키려면 JWT 바로 뒤에 느낌표를 추가하면 돼요 그 느낌표는 이러겠죠 TypeScript, 걱정 마요, 이미 확인했어요

## 내용
### 자막·본문 기반 핵심 내용
- TypeScript를 만족시키려면 JWT 바로 뒤에 느낌표를 추가하면 돼요 그 느낌표는 이러겠죠 TypeScript, 걱정 마요, 이미 확인했어요
- TypeScript는 환경 변수가 정의됐다고 절대 추정하지 않거든요 전적으로 가능합니다 응용 프로그램을 실행할 때 해당 환경 변수를 완전히 TypeScript는 우리 코드 내에서 그걸 설명하길 원해요
- 개인적으로 TypeScript가 잘못된 것 같아요 TypeScript가 좀 지나치게 조심하는 것 같아요 응용 프로그램이 처음 시작할 때 제가 이미 확인한 걸 모르거든요
- 제 디렉터리로 돌아갈게요 CRC 경로와 표지판을 찾아볼게요 간판 올리는 경로예요 처리기 특히 JSON Web 토큰을 생성하는 곳이죠 이 안에 하드코드된 문자열이 하드 코딩된 아주 중요한 정보 부분은 원치 않죠

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `JSON`, `TypeScript`, `JWT`, `MongoDB`

## 예시
`JSON`, `TypeScript`, `JWT`, `MongoDB`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Accessing Env Variables in a Pod**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19119828#overview)
