# Cookies and Encryption

## 개요
- 하지만 그건 큰 문제가 안 돼요 JSON 웹 토큰은 원래 조작에 강해요 악성 사용자가 JSON 웹 토큰 안의 정보를 수정하려 하면 JSON 웹 토큰이 유효하지 않다는 걸 바로 알 수 있어요 사용자가 그들 내부의 데이터를 조작하려 그게 우리가 신경 쓰는 거예요

## 내용
### 자막·본문 기반 핵심 내용
- 하지만 그건 큰 문제가 안 돼요 JSON 웹 토큰은 원래 조작에 강해요 악성 사용자가 JSON 웹 토큰 안의 정보를 수정하려 하면 JSON 웹 토큰이 유효하지 않다는 걸 바로 알 수 있어요 사용자가 그들 내부의 데이터를 조작하려 그게 우리가 신경 쓰는 거예요
- 인증과 관련된 모든 걸 처리하는 방법을 설정했어요 실제 인증 메커니즘으로는 JSON Web 토큰이 있어요 쿠키를 사용하면서 이 JSON Web 토큰을 저장하거나 관리할 거예요
- 암호화되지 않은 정보를 갖는다는 이 전체적인 아이디어가 어떤 식으로든 정말 두렵다면 이건 일반적으로 걱정할 큰 이유가 아니에요 보호된 정보를 JSON 웹 토큰 안에 저장하려는 게 아니라면요 어쨌든 그래선 안 되죠 쿠키 안에도요
- Rails의 Ruby로 가서 쿠키 내용을 읽어야 한다면 Ruby가 이 암호화 메서드도 지원할 수 있는지 확인해야 해요

### 구현 요소
- 강의에서 직접 언급하거나 사용하는 요소: `JSON`

## 예시
`JSON`가 등장하는 설정·코드 위치를 찾아 강의와 같은 순서로 확인한다.

## 요약
- 이 강의의 중심 주제는 **Cookies and Encryption**이며, 위 항목은 실제 강의 자막 또는 본문에서 추린 내용이다.
- [원본 강의](https://www.udemy.com/course/microservices-with-node-js-and-react/learn/lecture/19119758#overview)
