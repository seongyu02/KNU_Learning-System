# Introduction to Dash

## 개요
- 강좌: Data Visualization with Python
- 모듈:  Creating Dashboards with Plotly and Dash
- 재생 시간: 4분
- [MOOC 원본 강의](https://www.mooc.org/learn/python-for-data-visualization/lecture/BwaBv/introduction-to-dash)
- 이 비디오를 시청하고 나면 Dash가 무엇이고 그 용도에 대해 설명하실 수 있을 것입니다.
- Dash는 반응형 웹 기반 애플리케이션을 만들기 위한 오픈 소스 사용자 인터페이스 Python 라이브러리입니다.

## 내용
### 핵심 내용
- 이 비디오를 시청하고 나면 Dash가 무엇이고 그 용도에 대해 설명하실 수 있을 것입니다.
- Dash는 반응형 웹 기반 애플리케이션을 만들기 위한 오픈 소스 사용자 인터페이스 Python 라이브러리입니다.
- dash_HTML_COMPONENTS 라이브러리에는 모든 HTML 태그에 대한 구성 요소가 있습니다.
- 이 비디오에서는 Dash가 반응형 웹 기반 애플리케이션을 만들기 위한 오픈 소스 사용자 인터페이스 Python 라이브러리라는 것을 배웠습니다.
- Dash에는 코어 구성 요소와 HTML 구성 요소라는 두 가지 구성 요소가 있습니다.
- 대시_HTML_COMPONENTS 라이브러리에는 모든 HTML 태그에 대한 구성 요소가 있습니다.

### 한국어 Transcript

이 비디오를 시청하고 나면 Dash가 무엇이고 그 용도에 대해 설명하실 수 있을 것입니다. 대시 HTML 컴포넌트가 무엇인지 설명해 주세요. Dash는 반응형 웹 기반 애플리케이션을 만들기 위한 오픈 소스 사용자 인터페이스 Python 라이브러리입니다. 기업용으로 사용할 수 있으며 Plotly의 오픈 소스 도구 중 일류 멤버이기도 합니다. 대시 애플리케이션은 플라스크를 실행하고 HTTP 요청을 통해 JSON 패킷을 전달하는 웹 서버입니다.

js 를 사용하여 컴포넌트를 렌더링합니다. Dash는 애플리케이션을 만드는 데 필요한 모든 기술을 추상화하므로 Dash를 사용하여 그래픽 사용자 인터페이스를 쉽게 구축할 수 있습니다. 웹 브라우저에서 렌더링하여 서버에 배포할 수 있습니다. 코드를 UI에 바인딩하기 위한 간단한 반응형 데코레이터를 제공합니다. 기본적으로 모바일과 크로스 플랫폼 호환이 가능합니다.

비즈니스 질문에 답하는 애플리케이션을 만들 계획이라고 가정해 보겠습니다. 첫 번째 단계로, 애플리케이션의 레이아웃을 결정하고, 사용할 차트, 배치 위치를 결정해야 합니다. 예를 들어, 이를 Dash의 레이아웃 부분이라고 합니다. 두 번째 부분은 애플리케이션에 상호작용을 추가하는 것입니다. Dash에는 두 가지 구성 요소가 있습니다.

이 import 문을 사용하여 핵심 구성 요소를 DCC로 가져올 수 있습니다. 이 import 문을 사용하여 HTML 구성 요소를 HTML로 가져올 수 있습니다. dash_core_components는 React. js 라이브러리를 통해 자바스크립트, HTML, CSS로 생성된 상위 수준의 대화형 구성 요소에 대해 설명합니다. 핵심 구성 요소의 몇 가지 예로는 슬라이더, 입력 영역, 체크 항목, 날짜 선택기 만들기 등이 있습니다.

슬라이드 끝에 있는 참조 링크를 사용하여 다른 구성 요소를 탐색할 수 있습니다. dash_HTML_COMPONENTS 라이브러리에는 모든 HTML 태그에 대한 구성 요소가 있습니다. 대시 대시 HTML 대시 컴포넌트 라이브러리와 함께 Python 구조를 사용하여 레이아웃을 구성할 수 있습니다. dash_HTML_COMPONENTS 라이브러리는 모든 HTML 태그에 대한 클래스를 제공하며 키워드 인수는 스타일, 클래스 이름, ID와 같은 HTML 속성을 설명합니다. HTML이나 CSS에 대한 지식은 필요하지 않지만 대시보드의 스타일을 지정하는 데 도움이 될 수 있습니다.

이 비디오에서는 Dash가 반응형 웹 기반 애플리케이션을 만들기 위한 오픈 소스 사용자 인터페이스 Python 라이브러리라는 것을 배웠습니다. Dash는 애플리케이션을 만드는 데 필요한 모든 기술을 추상화하므로 Dash를 사용하여 그래픽 사용자 인터페이스를 쉽게 구축할 수 있습니다. Dash에는 코어 구성 요소와 HTML 구성 요소라는 두 가지 구성 요소가 있습니다. dash_core_components는 React. js 라이브러리를 통해 자바스크립트, HTML, CSS로 생성된 상위 수준의 대화형 구성 요소를 설명합니다.

대시_HTML_COMPONENTS 라이브러리에는 모든 HTML 태그에 대한 구성 요소가 있습니다.

## 예시
- 코드를 UI에 바인딩하기 위한 간단한 반응형 데코레이터를 제공합니다.
- 예를 들어, 이를 Dash의 레이아웃 부분이라고 합니다.

## 요약
- 이 비디오에서는 Dash가 반응형 웹 기반 애플리케이션을 만들기 위한 오픈 소스 사용자 인터페이스 Python 라이브러리라는 것을 배웠습니다.
- Dash에는 코어 구성 요소와 HTML 구성 요소라는 두 가지 구성 요소가 있습니다.
- 대시_HTML_COMPONENTS 라이브러리에는 모든 HTML 태그에 대한 구성 요소가 있습니다.

<details>
<summary>영문 Transcript 원문</summary>

Welcome to Introduction to Dash. After watching this video, you'll be able to explain what Dash is and its uses. Describe what Dash HTML components are. Dash is an open-source, user interface Python library for creating reactive web-based applications. It's both enterprise-ready and a first-class member of Plotly’s open-source tools.

Dash applications are web servers running Flask and communicating JSON packets over HTTP requests. Dash’s front end renders components using React. It's easy to build graphical user interfaces using Dash as it abstracts all technologies required to make the applications. Dash is declarative and reactive. It can be rendered in a web browser and deployed to servers.

It provides a simple reactive decorator for binding code to UI. They are inherently mobile and cross-platform-ready. Let's say you plan to create an application to answer business questions. As a first step, you need to determine the application’s layout, decide which chart to use, and where to place it. For example, this is called the layout part of Dash.

The second part is to add interactivity to the application. There are two components of Dash. First is the core components. We can import the core components as DCC using this import statement. Next is HTML components.

We can import HTML components as HTML using this import statement. Let's see them one by one. The dash_core_components describe higher-level interactive components generated with JavaScript, HTML, and CSS through the React. Some examples of core components are creating a slider, input area, check items, and date picker. You can explore other components using the reference link at the slide’s end.

The dash_HTML_components library has a component for every HTML tag. You can compose your layout using Python structures with the Dash Dash HTML Dash components library. The dash_HTML_components library provides classes for all HTML tags and the keyword arguments describe the HTML attributes like style, class name, and ID. No knowledge of HTML or CSS is required, but it can help style the dashboards. In this video, you learned that Dash is an open-source user interface Python library for creating reactive, web-based applications.

It's easy to build graphical user interfaces using Dash as it abstracts all technologies required to make the applications. There are two components of Dash: Core and HTML components. The dash_core_components describe higher-level interactive components generated with JavaScript, HTML, and CSS through the React. The dash_HTML_components library has a component for every HTML tag.

</details>
