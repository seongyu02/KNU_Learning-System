# Demo: Generative AI for Data Generation and Augmentation

## 개요
- 강좌: Generative AI: Elevate Your Data Science Career
- 모듈: Data Science and Generative AI
- 재생 시간: 7분
- [MOOC 원본 강의](https://www.mooc.org/learn/generative-ai-elevate-your-data-science-career/lecture/i30T7/demo-generative-ai-for-data-generation-and-augmentation)
- 이 동영상을 시청한 후에는 다음을 수행할 수 있습니다 제너레이티브 AI가 데이터 생성 및 증강에 유용한 도구인 이유 설명하기 제너레이티브 AI 기반 도구를 사용하여 합성 데이터 생성하기 그리고 제너레이티브 AI 기반 도구를 사용하여 데이터 증강하기 데이터 증강은 머신러닝 모델의 성능을 향상시키는 강력한 기술입니다 모델, 특히 모델의 학습 데이터 세트가 작거나 불균형할 때 성능을 향상시키는 강력한 기술입니다.
- 이는 기존 데이터 세트에서 수정된 데이터를 생성하여 수정된 데이터를 생성하여 학습 데이터 세트의 크기를 인위적으로 늘리는 과정입니다.

## 내용
### 핵심 내용
- 이 동영상을 시청한 후에는 다음을 수행할 수 있습니다 제너레이티브 AI가 데이터 생성 및 증강에 유용한 도구인 이유 설명하기 제너레이티브 AI 기반 도구를 사용하여 합성 데이터 생성하기 그리고 제너레이티브 AI 기반 도구를 사용하여 데이터 증강하기 데이터 증강은 머신러닝 모델의 성능을 향상시키는 강력한 기술입니다 모델, 특히 모델의 학습 데이터 세트가 작거나 불균형할 때 성능을 향상시키는 강력한 기술입니다.
- 이는 기존 데이터 세트에서 수정된 데이터를 생성하여 수정된 데이터를 생성하여 학습 데이터 세트의 크기를 인위적으로 늘리는 과정입니다.
- GPT 및 Copilot과 같은 생성형 AI 도구는 사실적인 텍스트 설명을 생성하여 반정형 데이터 세트를 효과적으로 보강할 수 있습니다 사실적인 텍스트 설명과 코드 스니펫을 생성하여 반정형 데이터세트를 효과적으로 보강할 수 있습니다.
- 생성형 AI 도구는 데이터 세트를 생성하는 방식이 다를 수 있습니다.
- 이 동영상에서 데이터 증강은 학습 데이터 세트의 크기를 인위적으로 훈련 데이터 세트의 크기를 인위적으로 늘리는 과정이라는 것을 배웠습니다.
- GPT와 Copilot과 같은 생성 AI 도구는 반정형 데이터 세트를 효과적으로 보강할 수 있습니다.

### 한국어 Transcript

데이터 생성 및 증강에 유용한 도구인 제너레이티브 AI에 관한 동영상에 오신 것을 환영합니다. 이 동영상을 시청한 후에는 다음을 수행할 수 있습니다 제너레이티브 AI가 데이터 생성 및 증강에 유용한 도구인 이유 설명하기 제너레이티브 AI 기반 도구를 사용하여 합성 데이터 생성하기 그리고 제너레이티브 AI 기반 도구를 사용하여 데이터 증강하기 데이터 증강은 머신러닝 모델의 성능을 향상시키는 강력한 기술입니다 모델, 특히 모델의 학습 데이터 세트가 작거나 불균형할 때 성능을 향상시키는 강력한 기술입니다. 이는 기존 데이터 세트에서 수정된 데이터를 생성하여 수정된 데이터를 생성하여 학습 데이터 세트의 크기를 인위적으로 늘리는 과정입니다. 데이터는 정형 데이터, 비정형 데이터, 반정형 데이터의 세 가지 범주로 분류할 수 있기 때문에 데이터로 분류할 수 있으므로 이러한 범주의 데이터를 생성하거나 보강하는 방법에는 여러 가지가 있을 수 있습니다. 이러한 각 범주에 사용할 수 있는 몇 가지 도구를 살펴보고 다음과 같이 살펴보겠습니다 제너레이티브 AI를 데이터 생성 및 증강에 얼마나 쉽게 활용할 수 있는지 살펴보겠습니다.

표 형식으로 저장되는 구조화된 데이터는 많은 머신 러닝 시스템의 기초가 됩니다 시스템의 기반이 됩니다. 트랜스포머 아키텍처의 조건부 생성적 적대 신경망인 CTGAN은 텍스트 이미지 변환을 위한 의 조건부 생성적 적대 신경망인 CTGAN과 합성 데이터 볼트(SDV)는 생성적 AI 기술의 예입니다 구조화된 데이터 세트를 보강하는 데 탁월합니다. 이러한 도구는 원본 데이터에서 볼 수 있는 통계적 특성과 상관관계를 모방한 합성 데이터를 생성합니다 모방한 합성 데이터를 생성합니다. 데이터 불균형, 누락된 값, 개인정보 보호 문제를 처리하는 데 특히 유용할 수 있습니다 유용합니다. 텍스트와 코드와 같은 반정형 데이터는 데이터 증강에 있어 고유한 과제를 안고 있습니다.

GPT 및 Copilot과 같은 생성형 AI 도구는 사실적인 텍스트 설명을 생성하여 반정형 데이터 세트를 효과적으로 보강할 수 있습니다 사실적인 텍스트 설명과 코드 스니펫을 생성하여 반정형 데이터세트를 효과적으로 보강할 수 있습니다. 이를 통해 자연어 처리 및 코드 생성 작업에서 머신 러닝 모델의 성능을 향상시킬 수 있습니다 및 코드 생성 작업에서 머신 러닝 모델의 성능을 향상시킬 수 있습니다. 이미지와 오디오를 포함한 비정형 데이터는 그 고유의 복잡성으로 인해 복잡성. StyleGAN2 및 BigGAN과 같은 생성형 AI 도구는 이미지 데이터를 증강하는 데 있어서 고해상도의 사실적인 이미지를 생성하여 이미지 데이터세트를 증강하는 데 놀라운 역량을 발휘합니다. 또한, 제너레이티브 AI는 새로운 오디오 샘플을 합성하여 오디오 데이터 세트를 보강하는 데 사용할 수 있습니다 오디오 샘플을 합성하여 오디오 데이터세트를 보강하는 데에도 사용할 수 있습니다.

데이터 증강 및 생성을 위한 몇 가지 제너레이티브 AI 툴을 확인해 보겠습니다. 유니버설 데이터 웹사이트(generate. io)로 이동하여 다음과 같이 진행하겠습니다 데이터 집합을 생성하라는 프롬프트로 시작합니다. 그래서 당뇨병 증상에 대한 환자 데이터 집합을 입력합니다. 프롬프트가 처리되는 데 시간이 걸릴 수 있습니다.

데이터가 생성되고 응답을 볼 수 있습니다. 생성된 데이터를 복사하여 다운로드할 수 있습니다. 다운로드를 선택하면 CSV 파일이 생성되어 저장됩니다. 생성형 AI 도구는 데이터 세트를 생성하는 방식이 다를 수 있습니다. 어떤 것은 효율적일 수도 있고 어떤 것은 추가 작업이 필요할 수도 있습니다.

무료로 제공되는 또 다른 도구인 ChatGPT를 사용해 보겠습니다 프롬프트에서 생성하려는 데이터의 기능을 지정하겠습니다. 속성을 화씨 온도, 온도 카테고리, 습도 백분율, 풍속(시속 마일), 풍향, 가시성, 추위, 강수량, 안개, 비 및 눈을 예 또는 아니오 범주, 월, 연도의 범주 유형으로 지정합니다 를 지정하여 100개의 관측값에 대한 데이터를 CSV 형식으로 생성해야 합니다. 그런 다음 이 프롬프트를 제출하면 샘플 데이터가 생성됩니다. 필요에 따라 값과 속성을 수정할 수 있습니다. 이 코드를 복사하여 Jupyter Notebook에서 실행할 수도 있습니다.

다른 GPT 도구에 동일한 프롬프트를 제공해 보겠습니다. com에서 BARD 웹 페이지를 열고 새 세션을 시작합니다. 이전과 동일한 프롬프트를 사용합니다. 그런 다음 프롬프트를 제출하면 데이터가 생성됩니다. 이 도구가 데이터를 생성하고 검토할 수 있도록 다른 초안 버전도 생성했습니다 을 생성합니다.

데이터를 복사하여 나중에 사용할 수도 있습니다. 다른 도구에서 데이터 세트를 업로드하고 매개 변수를 추가하여 데이터를 더 효과적으로 생성해 보겠습니다. MostlyAI 웹사이트(synthetic. 파일 업로드 탭에서 아이콘을 클릭하여 파일을 업로드한 다음, Daily Car Sales 데이터 세트를 선택합니다. 그런 다음 진행을 클릭하고 이 작업에 대한 학습 목표를 선택할 수 있습니다.

옵션은 정확도, 속도 및 터보입니다. 각각은 다른 목표를 희생하면서 한 가지 목표에 중점을 두며, 각 목표를 선택하면 하나를 선택하면 훈련 규모와 훈련 에포크 수가 변경되는 것을 볼 수 있습니다. 합성 데이터 세트 만들기를 클릭하면 에포크가 실행되며, 이 작업에는 상당한 시간이 걸릴 수 있습니다. 상태는 보류 중이라고 표시되고 생성되는 동안 진행 중이라고 표시됩니다. 상태 링크를 클릭하면 진행 상황을 확인할 수 있습니다.

이제 작업이 완료되었으니 증강된 데이터 집합을 살펴보겠습니다. 데이터 세트가 원본 데이터 세트와 동일한 수의 샘플을 생성했습니다. 데이터 세트를 다운로드하여 모델 훈련에 사용할 수 있습니다. 마지막으로 코딩을 사용하여 데이터를 생성해 보겠습니다. com에서 Collaboratory 웹사이트를 엽니다.

노트북에서 열면 먼저 CTGAN 모듈을 설치해야 합니다. 또한 테이블 평가기도 설치해야 합니다. 이 데모에서는 제공된 샘플 데이터 세트 중 하나인 CaliforniaHousingTrain. 왼쪽 창에서 파일 메뉴를 연 다음 샘플 데이터를 확장하면 여러 샘플 데이터 집합 중에서 를 선택할 수 있습니다. Pandas를 사용하여 이러한 샘플 CSV 파일 중 하나를 랩 노트북으로 가져옵니다.

다음으로, 이 코드를 사용하여 데이터에 CTGAN을 맞춥니다. 데이터에 범주형 특징이 있는 경우, 이를 목록으로 적합 함수에 전달해야 합니다. 200개의 에포크를 실행하는 데 시간이 좀 걸립니다. 샘플이 원본 데이터에 얼마나 가까운지 평가할 수 있습니다. 이를 위해서는 테이블 평가기를 가져와야 합니다.

그런 다음 원본 데이터와 샘플 데이터의 모양을 인쇄합니다. 그런 다음 가짜 데이터와 원본 데이터를 비교합니다. 그리고 평가 비주얼리제이션을 만들면 생성된 가짜 데이터가 이 원본 실제 데이터와 상당히 유사하다는 것을 보여줍니다. 이 동영상에서 데이터 증강은 학습 데이터 세트의 크기를 인위적으로 훈련 데이터 세트의 크기를 인위적으로 늘리는 과정이라는 것을 배웠습니다. 구조화된 데이터 세트의 증강에 탁월한 제너레이티브 AI 기술의 예로 CTGAN과 SDV가 있습니다 데이터 세트.

GPT와 Copilot과 같은 생성 AI 도구는 반정형 데이터 세트를 효과적으로 보강할 수 있습니다. StyleGAN2 및 BigGAN과 같은 생성 AI 도구는 이미지 데이터 세트를 보강하고, SoundGAN은 새로운 오디오 샘플을 새로운 오디오 샘플을 합성합니다.

## 예시
- 텍스트와 코드와 같은 반정형 데이터는 데이터 증강에 있어 고유한 과제를 안고 있습니다.
- GPT 및 Copilot과 같은 생성형 AI 도구는 사실적인 텍스트 설명을 생성하여 반정형 데이터 세트를 효과적으로 보강할 수 있습니다 사실적인 텍스트 설명과 코드 스니펫을 생성하여 반정형 데이터세트를 효과적으로 보강할 수 있습니다.
- 이를 통해 자연어 처리 및 코드 생성 작업에서 머신 러닝 모델의 성능을 향상시킬 수 있습니다 및 코드 생성 작업에서 머신 러닝 모델의 성능을 향상시킬 수 있습니다.
- 이 코드를 복사하여 Jupyter Notebook에서 실행할 수도 있습니다.

## 요약
- 생성형 AI 도구는 데이터 세트를 생성하는 방식이 다를 수 있습니다.
- 이 동영상에서 데이터 증강은 학습 데이터 세트의 크기를 인위적으로 훈련 데이터 세트의 크기를 인위적으로 늘리는 과정이라는 것을 배웠습니다.
- GPT와 Copilot과 같은 생성 AI 도구는 반정형 데이터 세트를 효과적으로 보강할 수 있습니다.

<details>
<summary>영문 Transcript 원문</summary>

Welcome to this video on Generative AI as a useful tool for data generation and augmentation. After watching this video, you'll be able to Explain how Generative AI is a useful tool for data generation and augmentation Generate synthetic data using a generative AI-based tool And augment data using a generative AI-based tool Data augmentation is a powerful technique for improving the performance of machine learning models, especially when the training datasets of the models are small or imbalanced. It is the process of artificially increasing the size of a training dataset by creating modified data from the existing one. Since data can be categorized into three categories, structured data, unstructured data, and semi-structured data, there can be many ways to generate these categories of data or augment it. Let's dive into some of the tools available for each of these categories and investigate how easily Generative AI can be leveraged for data generation and augmentation.

Structured data, often saved in tabular formats, serves as a foundation for many machine learning systems. CTGAN, which is a conditional, generative adversarial network of transformer architecture for text-to-image, and Synthetic Data Vault, or SDV, are examples of generative AI techniques which excel in augmenting structured datasets. These tools generate synthetic data that mimics the statistical traits and correlations seen in the original data. To handle data imbalances, missing values, and privacy problems, this can be especially helpful. Semi-structured data, such as text and code, poses unique challenges for data augmentation.

Generative AI tools like GPT and Copilot can effectively augment semi-structured datasets by generating realistic text descriptions and code snippets. This can enhance the performance of machine learning models in natural language processing and code generation tasks. Unstructured data, including images and audio, is often difficult to augment due to its inherent complexity. Generative AI tools like StyleGAN2 and BigGAN have demonstrated remarkable capabilities in augmenting image datasets by generating high-resolution, realistic images. In addition, generative AI can be used to augment audio datasets by synthesizing new audio samples, for instance, SoundGAN by NVIDIA.

Let's check out some generative AI tools for data augmentation and generation. We'll navigate to the Universal Data website at generate. io, and we'll begin with a prompt to create the dataset. So we type the prompt, patient dataset for the symptoms of diabetes. As of now, we will skip specifying the fields.

Then we generate the data. The prompt might take a while to process. The data is generated, and we can view the response. We can copy and download the generated data. If we choose Download, a CSV file will be generated and saved.

Generative AI tools may have different ways of creating datasets. Some may be efficient, while others would need extra work. Let's use another tool, ChatGPT, available free of charge, and this time we will specify the features of the data we want to be generated in the prompt. So we type the prompt as follows. Create a dataset with attributes as temperature in Fahrenheit, temperature category, humidity in percentage, wind speed in miles per hour, wind direction, visibility, windchill, precipitation, fog, rain, and snow as categorical type in yes or no categories, month, year, and specify the data has to be created for 100 observations in a CSV format.

Then we submit this prompt, and the sample data is generated. We could modify the values and attributes based on our specific needs. We could also copy this code and run it in a Jupyter notebook. Let's provide the same prompt to another GPT tool. We'll open the BARD web page at bard.

com and start a new session. We'll use the same prompt as before. Then we submit the prompt and the data is generated. Notice that this tool has generated the data and also generated some other draft versions for us to review. We could also copy the data to use it further.

In a different tool, let's upload a dataset and add parameters to generate data more effectively. We'll open the MostlyAI website at synthetic. On the Upload Files tab, we click the icon to upload files, then select the Daily Car Sales dataset. We then click Proceed, and we can select a training goal for this job. Options are Accuracy, Speed, and Turbo.

Each of these puts a focus on one goal at the cost of the other, and as we select each one we can see that the training size and training epochs number will change. When we click Create Synthetic Dataset, the epochs will be run, which can take quite some time. The status will say Pending, and then in progress while it's being generated. If you click the status link, you can see the progress. Now it's finished, let's have a look at the augmented dataset.

The dataset has generated the same number of samples as there were in the original dataset. We can download the dataset and use it for our model training. Finally, let's use coding to generate data. We'll open the Collaboratory website at collab. We open in a notebook, and first we need to install the CTGAN module.

We will also need to install the Table Evaluator. For this demonstration, we will use one of the provided sample datasets called CaliforniaHousingTrain. If we open the Files menu in the left pane, and then expand the sample data, we can choose from several sample datasets. Using Pandas, we'll import one of these sample CSV files into the lab notebook. Next, we fit CTGAN on the data using this code.

We need to import CTGAN. If our data has categorical features, we must pass them as a list to the fit function. Let's run this code. It will take a while to run 200 epochs. Now we'll save the generated samples.

We can evaluate the samples on how close they are to the original data. For that, we will need to import Table Evaluator. Then we print the shapes of the original and sample data. Then we'll make a comparison of the fake data and the original data. And if we create evaluation visualizations, they will show that the generated fake data is fairly similar to the original real data.

In this video, you learned that Data Augmentation is the process of artificially increasing the size of a training dataset by creating modified data from the existing one. CTGAN and SDV are examples of generative AI techniques that excel in augmenting structured datasets. Generative AI tools like GPT and Copilot can effectively augment semi-structured datasets. Generative AI tools like StyleGAN2 and BigGAN augment image datasets while SoundGAN synthesizes new audio samples.

</details>
