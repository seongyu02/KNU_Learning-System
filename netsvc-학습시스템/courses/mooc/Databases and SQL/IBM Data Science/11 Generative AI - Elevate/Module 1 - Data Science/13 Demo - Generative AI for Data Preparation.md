# Demo: Generative AI for Data Preparation

## 개요
- 강좌: Generative AI: Elevate Your Data Science Career
- 모듈: Data Science and Generative AI
- 재생 시간: 6분
- [MOOC 원본 강의](https://www.mooc.org/learn/generative-ai-elevate-your-data-science-career/lecture/Gcgal/demo-generative-ai-for-data-preparation)
- 이 동영상을 시청한 후에는 누락된 값을 대체하고 데이터에서 이상값을 찾고, 조인을 사용하여 여러 데이터 테이블을 병합하고, 데이터를 필터링하고, AI 어시스턴트를 사용하여 데이터를 분석하고, if-then 규칙을 만드는 방법을 배울 수 있습니다.
- 데이터 준비는 데이터 분석 프로세스에서 중요한 단계입니다, 원시 데이터를 정리, 변환 및 추가 분석 및 모델링에 적합하도록 원시 데이터를 구성하는 것을 포함합니다.

## 내용
### 핵심 내용
- 이 동영상을 시청한 후에는 누락된 값을 대체하고 데이터에서 이상값을 찾고, 조인을 사용하여 여러 데이터 테이블을 병합하고, 데이터를 필터링하고, AI 어시스턴트를 사용하여 데이터를 분석하고, if-then 규칙을 만드는 방법을 배울 수 있습니다.
- 데이터 준비는 데이터 분석 프로세스에서 중요한 단계입니다, 원시 데이터를 정리, 변환 및 추가 분석 및 모델링에 적합하도록 원시 데이터를 구성하는 것을 포함합니다.
- 이제 다음과 같은 간단한 프롬프트를 작성하여 이 누락된 값을 처리해 보겠습니다 온도 F의 누락된 값을 온도 F의 평균으로 대체합니다.
- 때때로 AI 어시스턴트로부터 제안 탭에서 제안 탭에서 누락된 값이나 데이터 집합의 다른 테이블에 대한 다른 제안을 받을 수도 있습니다.
- 하지만 AI 어시스턴트에게 구체적인 질문을 할 수도 있습니다, 예를 들어, 여기 일반 채팅에서 온도 열의 누락된 값을 어떻게 처리할지 물어보겠습니다 온도 열의 누락된 값을 처리하는 방법을 물어보겠습니다.
- 이 동영상에서는 데이터에서 누락된 값을 대체할 수 있다는 것을 배웠습니다.

### 한국어 Transcript

데이터 준비를 위한 생성형 AI에 오신 것을 환영합니다. 이 동영상을 시청한 후에는 누락된 값을 대체하고 데이터에서 이상값을 찾고, 조인을 사용하여 여러 데이터 테이블을 병합하고, 데이터를 필터링하고, AI 어시스턴트를 사용하여 데이터를 분석하고, if-then 규칙을 만드는 방법을 배울 수 있습니다. 데이터 준비는 데이터 분석 프로세스에서 중요한 단계입니다, 원시 데이터를 정리, 변환 및 추가 분석 및 모델링에 적합하도록 원시 데이터를 구성하는 것을 포함합니다. 이 단계를 통해 데이터의 정확성, 신뢰성, 일관성을 보장할 수 있습니다. 이 동영상에서는 제너레이티브 AI가 어떻게 데이터를 더 쉽고 빠르게 준비할 수 있는지 보여주는 두 가지 데모 을 통해 데이터 전문가가 데이터를 더 쉽고 빠르게 준비하는 방법을 두 가지 데모로 보여드립니다.

첫 번째 데모에서는 ChatCSV 생성 AI 도구를 사용합니다, 개인 데이터 분석 도우미 역할을 하는 웹 기반 도구입니다, 채팅 세션에 CSV 파일을 첨부하고 질문할 수 있는 웹 기반 도구입니다. 채팅을 시작하려면 먼저 데이터 세트를 첨부해야 합니다, 우리가 사용할 데이터 세트는 Daily_Car_Sales. 데이터 세트가 업로드되면 GPT 모델에 설명과 기본 정보가 표시됩니다 기본 정보를 표시합니다. 또한 생성 AI 도구는 결측값이 포함된 열에 대한 추가 정보를 제공합니다 열에 대한 추가 정보도 제공합니다. 이제 다음과 같은 간단한 프롬프트를 작성하여 이 누락된 값을 처리해 보겠습니다 온도 F의 누락된 값을 온도 F의 평균으로 대체합니다.

업데이트된 데이터는 곧바로 준비되며, 누락된 값은 다음과 같습니다 의 결측값이 평균값으로 대체된 업데이트된 데이터가 즉시 준비됩니다. 데이터 집합의 이상값을 확인해 보겠습니다, gPT가 각 열에 대한 박스 플롯을 어떻게 생성했는지 확인해 보겠습니다. 여기에서 여러 데이터 열에 다음과 같이 표시된 이상값이 있는 것을 볼 수 있습니다 로 표시된 이상값이 있음을 알 수 있습니다. 생성형 AI를 사용하면 데이터 준비 작업을 쉽게 할 수 있습니다. 다른 플랫폼으로 이동하여 데이터 준비 단계를 살펴보겠습니다.

이 무료 커뮤니티 버전의 Tomat. AI를 사용하면 제한 없이 데이터를 탐색하고 제한 없이 데이터를 분석할 수 있습니다. 먼저 CSV 파일을 열고 위치에서 끌어다 놓거나 찾아서 업로드할 수 있습니다. 페이지 오른쪽 상단에서 카탈로그에 추가를 클릭합니다. 이제 탐색기로 이동하여 데이터 세트의 각 열 오른쪽 창에서 통계를 볼 수 있습니다.

각 기상 조건 카테고리의 평균 기온을 확인해 보겠습니다, 이를 위해 기상 조건별로 그룹화합니다, 를 클릭한 다음 값에 대한 온도 F 열을 선택하고 평균을 선택합니다. 대화 상자 외부를 클릭하면 업데이트된 테이블을 볼 수 있습니다 모든 다른 기상 조건의 평균값을 표시합니다. 이제 한 걸음 더 나아가 데이터를 흐름으로 변환해 보겠습니다. 이제 데이터 열에 필터를 사용해 보겠습니다. csv라는 다른 데이터 테이블을 업로드하겠습니다.

만들기를 클릭하여 변경 사항을 저장합니다. 이제 이 두 테이블을 병합할 것이므로 조인을 선택하고 두 테이블 아이콘을 조인 아이콘에 연결합니다. 조인 유형은 왼쪽 조인을 선택하고 두 키에 대해 딜러 ID를 선택하고 조인을 만듭니다. 데이터에 필터를 포함할 수도 있습니다. 날씨 조건이 구름이 흩어져 있는 데이터를 필터링해 보겠습니다.

이제 데이터에서 구름이 흩어져 있는 행만 볼 수 있습니다. 때때로 AI 어시스턴트로부터 제안 탭에서 제안 탭에서 누락된 값이나 데이터 집합의 다른 테이블에 대한 다른 제안을 받을 수도 있습니다. 하지만 AI 어시스턴트에게 구체적인 질문을 할 수도 있습니다, 예를 들어, 여기 일반 채팅에서 온도 열의 누락된 값을 어떻게 처리할지 물어보겠습니다 온도 열의 누락된 값을 처리하는 방법을 물어보겠습니다. AI 어시스턴트의 제안 중 하나는 누락된 값을 상수로 채우라는 것입니다 를 상수(예: 평균)로 채우는 것입니다. If-then 규칙을 사용하여 이 작업을 수행해 보겠습니다.

누락된 온도 값을 평균 열 값으로 대체하겠습니다. then을 선택한 다음 이를 수행하는 함수를 작성합니다. 온도 F 열에 대해 is missing 조건을 선택하고 조건이 참이면 온도의 평균값을 어트리뷰션합니다. 그렇지 않으면 행에 있는 실제 온도 값을 그대로 사용합니다. 이제 온도 열을 덮어쓰도록 선택하고 를 선택한 다음 규칙을 만듭니다.

이제 조인 필터에 연결된 if 항목을 볼 수 있습니다. 마지막으로, 흐름의 출력을 포함하여 처리된 데이터의 CSV 이 처리된 데이터의 CSV를 생성하기 위해 로컬 파일을 선택하고 생성 위치를 지정하면 준비된 데이터라고 부릅니다. 이제 흐름이 작업을 수행할 준비가 되었으므로 작업 실행 아이콘을 클릭하고 를 클릭하고 CSV가 준비될 때까지 기다립니다. 시간이 꽤 걸릴 수 있습니다, 하지만 작업이 완료되면 준비된 데이터가 포함된 CSV를 볼 수 있습니다. 이 동영상에서는 데이터에서 누락된 값을 대체할 수 있다는 것을 배웠습니다.

데이터의 이상값을 관리할 수 있고, 각 데이터 카테고리의 평균값을 찾을 수 있습니다 필터를 적용하고, 데이터 테이블을 병합하고, 열을 삭제할 수 있습니다, 그리고 처리된 데이터의 CSV 파일을 생성할 수 있습니다.

## 예시
- 이 동영상에서는 제너레이티브 AI가 어떻게 데이터를 더 쉽고 빠르게 준비할 수 있는지 보여주는 두 가지 데모 을 통해 데이터 전문가가 데이터를 더 쉽고 빠르게 준비하는 방법을 두 가지 데모로 보여드립니다.
- 첫 번째 데모에서는 ChatCSV 생성 AI 도구를 사용합니다, 개인 데이터 분석 도우미 역할을 하는 웹 기반 도구입니다, 채팅 세션에 CSV 파일을 첨부하고 질문할 수 있는 웹 기반 도구입니다.
- 하지만 AI 어시스턴트에게 구체적인 질문을 할 수도 있습니다, 예를 들어, 여기 일반 채팅에서 온도 열의 누락된 값을 어떻게 처리할지 물어보겠습니다 온도 열의 누락된 값을 처리하는 방법을 물어보겠습니다.

## 요약
- 때때로 AI 어시스턴트로부터 제안 탭에서 제안 탭에서 누락된 값이나 데이터 집합의 다른 테이블에 대한 다른 제안을 받을 수도 있습니다.
- 하지만 AI 어시스턴트에게 구체적인 질문을 할 수도 있습니다, 예를 들어, 여기 일반 채팅에서 온도 열의 누락된 값을 어떻게 처리할지 물어보겠습니다 온도 열의 누락된 값을 처리하는 방법을 물어보겠습니다.
- 이 동영상에서는 데이터에서 누락된 값을 대체할 수 있다는 것을 배웠습니다.

<details>
<summary>영문 Transcript 원문</summary>

Welcome to Generative AI for Data Preparation. After watching this video, you'll be able to replace missing values and find outliers in data, merge multiple data tables by using a join, filter data, use the AI assistant to analyze data and create an if-then rule. Data preparation is a crucial step in the process of data analytics, which involves cleaning, transforming, and organizing RAW data to make it suitable for further analysis and modeling. This step ensures that the data is accurate, reliable, and consistent. In this video, we will walk you through two demonstrations of how generative AI makes data preparation easier and quicker for data professionals.

In this first demo, we will use the ChatCSV generative AI tool, which is a web-based tool that acts as a personal data analyst assistant, enabling you to attach any CSV file to a chat session and ask questions. Let's begin by initiating a chat, we need to attach our data set, the data set we will use is Daily_Car_Sales. Once the data set is uploaded, the GPT model displays the description and basic information. The generative AI tool also provides additional information on the columns that contain missing values. Now we will try to handle this missing value by writing a simple prompt like replace missing values of temperature F with the mean of temperature F.

The updated data is ready in no time, with missing values of the temperature column replaced with the average values. Let's check the outliers in the data set, see how the GPT has produced the box plots for each of the columns. Here we can see that several of the data columns have outliers represented by the black dots. Generative AI makes it easy to work with data preparation tasks. Let's move on to another platform and explore data preparation steps.

This free community version of Tomat. AI allows you to explore and analyze your data without limitations. Let's upload the data set, first we open our CSV file and we can either drag and drop it from its location or browse to upload it. In the top right of the page, we click add to catalog. We are now in explorer and can see statistics in the right hand pane of each column in the data set.

Let's see the average temperature for each category of weather condition, to do this, we group by weather condition, then select the temperature F column for the value and choose average. When we click outside the dialog box, we can see the updated table displaying average values of all different weather conditions. Now we will go further and convert the data to a flow. Let's now try using a filter on the data columns. First, we'll upload another data table called Dealer_ID_Names.

Here's the data file. We will click create to save our changes. Now we're going to merge these two tables so we choose join and connect the two table icons to the join icon. For the type of join, we will pick left join and we select dealer ID for both keys and create the join. We can also include filters in our data.

Let's filter the data where the weather condition is scattered clouds. Now we can only see rows with scattered clouds visible in the data. You may also sometimes get suggestions from the AI assistant on the suggestions tab for things like missing values or other suggestions on different tables in your data set. But we can also ask the AI assistant specific questions, for example, in the general chat here we're going to ask the GPT how to deal with missing values in the temperature column. One of the suggestions from the AI assistant is to fill the missing values with a constant, for example the average.

Let's do this by using an if-then rule. We'll replace the missing temperature values with an average column value. then from the new menu and then write the function to do so. We select the is missing condition for the temperature F column and if this is true we want the average value of the temperature to be attributed. Otherwise, we want the actual temperature value as it is in the row.

Now we will choose to overwrite the temperature column and then create the rule. So now we can see an if item connected to the join filter. Lastly, we need to include the output of the flow to generate a CSV of this processed data, we select local file and specify where to create it and we'll call it prepared data. Now the flow is ready for the job, so we click the run job icon and wait till the CSV is prepared. This can take quite some time, but once it's finished we can see the CSV containing the prepared data.

In this video, you learned that you can replace missing values in data. You can manage outliers in data, you can find average values for each data category, you can apply filters, merge data tables, delete columns, and you can generate a CSV file of processed data.

</details>
