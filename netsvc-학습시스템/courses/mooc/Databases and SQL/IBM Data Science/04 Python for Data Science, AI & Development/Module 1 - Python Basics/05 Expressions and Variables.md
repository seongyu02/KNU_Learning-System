# Expressions and Variables

## 개요
- 강좌: Python for Data Science, AI & Development
- 모듈: Python Basics
- 재생 시간: 4분
- [MOOC 원본 강의](https://www.mooc.org/learn/python-for-applied-data-science-ai/lecture/gjVDL/expressions-and-variables)
- 표현식은 컴퓨터가 수행하는 작업 유형을 설명합니다.
- 예를 들어, 여러 숫자를 더하는 것과 같은 기본 산술 연산을 들 수 있습니다.

## 내용
### 핵심 내용
- 표현식은 컴퓨터가 수행하는 작업 유형을 설명합니다.
- 예를 들어, 여러 숫자를 더하는 것과 같은 기본 산술 연산을 들 수 있습니다.
- 별표를 사용하여 곱셈 연산을 수행할 수 있습니다.
- 변수를 사용하여 값을 저장할 수 있습니다.
- 또한 x에서 연산을 수행하고 결과를 새 변수 y에 저장할 수 있습니다.
- 변수 내에서도 type 명령을 사용할 수 있습니다.

### 한국어 Transcript

이 비디오에서는 표현식과 변수를 다룰 것입니다. 표현식은 컴퓨터가 수행하는 작업 유형을 설명합니다. 표현식은 Python이 수행하는 연산입니다. 예를 들어, 여러 숫자를 더하는 것과 같은 기본 산술 연산을 들 수 있습니다. 우리는 숫자를 피연산자라고 부르고, 수학 기호 (이 경우에는 더하기) 를 연산자라고 합니다.

뺄셈 기호를 사용하여 뺄셈과 같은 연산을 수행할 수 있습니다. 별표를 사용하여 곱셈 연산을 수행할 수 있습니다. 이 경우 연산은 음수와 별표로 표시됩니다. 포워드 슬래시를 사용하여 나누기를 수행할 수도 있습니다. 25를 6으로 나눈 값은 약 4.167입니다.

이 강좌에서 사용할 버전인 Python 3에서는 둘 다 float가 됩니다. 정수 나누기에 이중 슬래시를 사용할 수 있습니다. 경우에 따라 결과가 정규 나눗셈과 동일하지 않다는 점에 유의하십시오. Python은 수학 표현식을 수행할 때 수학 규칙을 따릅니다. 두 경우 모두 Python은 곱셈을 수행한 다음 덧셈을 수행하여 최종 결과를 얻습니다.

Python으로 할 수 있는 작업은 훨씬 더 많습니다. 실습에서 더 많은 예제를 확인하세요. 또한 과정 전반에 걸쳐 더 복잡한 작업을 다룰 예정입니다. 괄호 안의 표현식이 먼저 수행됩니다. 변수를 사용하여 값을 저장할 수 있습니다.

이 경우 대입 연산자, 즉 등호를 사용하여 myunderscore 변수 변수에 값 1을 할당합니다. 그런 다음 변수의 정확한 이름을 입력하여 코드의 다른 곳에서 값을 사용할 수 있습니다. 할당 연산자를 사용하여 밑줄 변수에 새 값을 할당할 수 있습니다. 변수의 이전 값은 중요하지 않습니다. 표현식의 결과를 저장할 수 있습니다.

예를 들어, 여러 값을 추가하고 결과를 x에 할당합니다. 또한 x에서 연산을 수행하고 결과를 새 변수 y에 저장할 수 있습니다. x에 연산을 수행하고 값 x를 할당할 수도 있습니다. 이제 변수 x의 값은 2.666입니다. 이전과 마찬가지로 x의 이전 값은 중요하지 않습니다.

변수 내에서도 type 명령을 사용할 수 있습니다. 변수가 무엇을 하는지 추적할 필요가 없도록 의미 있는 변수 이름을 사용하는 것이 좋습니다. 강조 표시된 예제의 분 수를 다음 음악 데이터 세트의 시간 수로 변환한다고 가정해 보겠습니다. 총 시간 (분), 총 밑줄 최소값이 포함된 변수를 호출합니다. 새 단어의 시작을 나타내는 데 밑줄을 사용하는 것이 일반적입니다.

총 시간, 총 밑줄 시간을 포함하는 변수를 호출합니다. 총 밑줄 최소값을 60으로 나누어 총 시간을 구할 수 있습니다. 첫 번째 변수의 값을 수정하면 변수 값이 변경됩니다. 최종 결과 값은 그에 따라 변경되지만 나머지 코드는 수정할 필요가 없습니다.

## 예시
- 예를 들어, 여러 숫자를 더하는 것과 같은 기본 산술 연산을 들 수 있습니다.
- 실습에서 더 많은 예제를 확인하세요.
- 그런 다음 변수의 정확한 이름을 입력하여 코드의 다른 곳에서 값을 사용할 수 있습니다.
- 예를 들어, 여러 값을 추가하고 결과를 x에 할당합니다.

## 요약
- 변수를 사용하여 값을 저장할 수 있습니다.
- 또한 x에서 연산을 수행하고 결과를 새 변수 y에 저장할 수 있습니다.
- 변수 내에서도 type 명령을 사용할 수 있습니다.

<details>
<summary>영문 Transcript 원문</summary>

In this video, we'll cover expressions and variables. Expressions describe a type of operation the computers perform. Expressions are operations the Python performs. For example, basic arithmetic operations like adding multiple numbers. The result, in this case, is 160.

We call the numbers operands, and the math symbols, in this case addition, are called operators. We can perform operations such as subtraction using the subtraction sign. In this case, the result is a negative number. We can perform multiplication operations using the asterisk. In this case, the operations are given by negative and asterisk.

We can also perform division with the forward slash. 25 divided by 5 is 5. 25 divided by 6 is approximately 4.167. In Python 3, the version we will be using in this course, both will result in a float. We can use the double slash for integer division, where the result is rounded.

Be aware, in some cases, the results are not the same as regular division. Python follows mathematical conventions when performing mathematical expressions. The following operations are in a different order. In both cases, Python performs multiplication, then addition to obtain the final result. There are a lot more operations you can do with Python.

Check the labs for more examples. We will also be covering more complex operations throughout the course. The expressions in the parentheses are performed first. We then multiply the result by 60. Now let's look at variables.

We can use variables to store values. In this case, we assign a value of 1 to the variable my underscore variable using the assignment operator, i. We can then use the value somewhere else in the code by typing the exact name of the variable. We can assign a new value to my underscore variable using the assignment operator. We assign a value of 10.

The variable now has a value of 10. The old value of the variable is not important. We can store the results of expressions. For example, we add several values and assign the result to x. x now stores the result.

We can also perform operations on x and save the result to a new variable y. y now has a value of 2.666. We could also perform operations on x and assign the value x. The variable x now has a value 2.666. As before, the old value of x is not important.

We can use the type command in variables as well. It's good practice to use meaningful variable names so you don't have to keep track of what the variable is doing. Let's say we would like to convert the number of minutes in the highlighted examples to number of hours in the following music data set. We call the variable that contains the total number of minutes, total underscore min. It's common to use the underscore to represent the start of a new word.

You could also use a capital letter. We call the variable that contains the total number of hours, total underscore hour. We can obtain the total number of hours by dividing total underscore min by 60. The result is approximately 2.367 hours. If we modify the value of the first variable, the value of the variable will change.

The final result values change accordingly, but we do not have to modify the rest of the code.

</details>
