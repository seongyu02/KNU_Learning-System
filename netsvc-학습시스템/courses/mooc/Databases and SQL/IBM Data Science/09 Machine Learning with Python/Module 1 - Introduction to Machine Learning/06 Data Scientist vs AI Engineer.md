# Data Scientist vs AI Engineer

## 개요
- 강좌: Machine Learning with Python
- 모듈: Introduction to Machine Learning
- 재생 시간: 11분
- [MOOC 원본 강의](https://www.mooc.org/learn/machine-learning-with-python/lecture/6mQxG/data-scientist-vs-ai-engineer)
- 수년 동안 데이터 과학은 21세기의 가장 섹시한 직업으로 불렸지만, 최근에는 AI 엔지니어라는 타이틀을 놓고 경쟁하는 새로운 직업이 있는 것 같습니다.
- 저는 전직 데이터 과학자로 IBM에서 AI 엔지니어로 전향했습니다.

## 내용
### 핵심 내용
- 수년 동안 데이터 과학은 21세기의 가장 섹시한 직업으로 불렸지만, 최근에는 AI 엔지니어라는 타이틀을 놓고 경쟁하는 새로운 직업이 있는 것 같습니다.
- 저는 전직 데이터 과학자로 IBM에서 AI 엔지니어로 전향했습니다.
- 이러한 질문에 답하기 위해 데이터 과학자의 업무가 AI 엔지니어, 특히 제너레이티브 AI 엔지니어와 다른 네 가지 주요 영역을 설명하겠습니다.
- 반면에 AI 엔지니어는 AI 시스템 빌더라고 생각하시면 됩니다.
- 이제 AI 엔지니어링에 대해 설명하자면, AI 엔지니어가 연구하는 주요 사용 사례 중 하나는 규범적 사용 사례라고 불리는데, 이는 모두 최선의 행동 방침을 선택하는 것입니다.
- 따라서 이것들은 대규모 AI 애플리케이션을 구축하는 데 사용할 수 있는 빌딩 블록의 몇 가지 예일 뿐입니다.

### 한국어 Transcript

수년 동안 데이터 과학은 21세기의 가장 섹시한 직업으로 불렸지만, 최근에는 AI 엔지니어라는 타이틀을 놓고 경쟁하는 새로운 직업이 있는 것 같습니다. 그럼 이 동네에 새로 온 애들은 대체 누구야? 그들은 단지 변장한 데이터 과학자일까요? 저는 전직 데이터 과학자로 IBM에서 AI 엔지니어로 전향했습니다. 이러한 질문에 답하기 위해 데이터 과학자의 업무가 AI 엔지니어, 특히 제너레이티브 AI 엔지니어와 다른 네 가지 주요 영역을 설명하겠습니다.

하지만 이러한 차이점에 대해 자세히 살펴보기 전에 먼저 업계에서 어떤 일이 벌어지고 있는지 더 많이 이해해야 합니다. 따라서 전통적으로 데이터 과학자는 분석을 위해 항상 AI 모델을 사용해 왔습니다. 제너레이티브 AI의 출현으로 AI가 할 수 있는 일의 경계가 이전에는 볼 수 없었던 방식으로 넓어지고 있습니다. 그래서 이러한 혁신은 너무나 획기적이어서 제너레이티브 AI는 고유한 분야로 분리되었습니다. 우리는 이를 AI 엔지니어링이라고 부릅니다.

자, 이제 환경을 이해했으니 차이점에 대해 알아보겠습니다. 첫 번째 차이점은 사용 사례에 있습니다. 따라서 매우 높은 수준에서 데이터 과학자는 데이터 스토리텔러라고 생각하시면 됩니다. 그들은 엄청난 양의 복잡한 현실 세계 데이터를 가져와서 수학적 모델을 사용하여 이 데이터를 인사이트로 변환합니다. 반면에 AI 엔지니어는 AI 시스템 빌더라고 생각하시면 됩니다.

이들은 기반 모델을 사용하여 비즈니스 프로세스를 혁신하는 데 도움이 되는 제너레이티브 AI 시스템을 구축합니다. 따라서 데이터 과학자는 훌륭한 스토리텔러이기 때문에 과거를 설명하기 위해 많은 서술적 분석을 사용합니다. 한 가지 예로 탐색적 데이터 분석 (EDA) 을 들 수 있습니다. 탐색적 데이터 분석 (EDA) 은 데이터를 그래프로 표시하고 통계적 추론을 수행하는 것입니다. 또한 고객 세분화와 같은 유사한 특성을 기반으로 유사한 데이터 포인트를 그룹화하는 클러스터링 (Clustering) 을 통해서도 이를 수행할 수 있습니다.

이제 모든 좋은 이야기에는 다음에 무엇이 나올지 알아내려고 애쓰는 독자가 있습니다. 그래서 예측 가능한 사용 사례가 필요하죠. 그러나 책과 달리 데이터 과학자는 용어가 이미 쓰여진 것이 아니기 때문에 기계 학습 모델이라는 것을 사용하여 예측을 해야 합니다. 회귀 모델은 온도 또는 수익과 같은 수치값을 예측합니다. 이러한 모델의 또 다른 유형은 성공 또는 실패와 같은 범주형 값을 예측하는 분류 모델입니다.

이제 AI 엔지니어링에 대해 설명하자면, AI 엔지니어가 연구하는 주요 사용 사례 중 하나는 규범적 사용 사례라고 불리는데, 이는 모두 최선의 행동 방침을 선택하는 것입니다. 이러한 예로는 의사결정 최적화라는 기술이 있습니다. 이를 통해 기업은 일련의 가능한 조치를 평가한 다음 일련의 요구 사항 또는 표준을 기반으로 가장 최적의 경로를 선택할 수 있습니다. 규범적 사용 사례의 또 다른 예는 추천 엔진을 만드는 것입니다. 예를 들어, 여기에는 특정 고객층을 대상으로 한 타겟 마케팅 캠페인을 제안하는 것이 포함될 수 있습니다.

규범적 사용 사례 외에도 생성적 사용 사례도 있기 때문에 제너레이티브 AI라는 이름이 붙었습니다. 이제 파운데이션 모델을 통해 코딩 어시스턴트나 디지털 어드바이저와 같은 지능형 어시스턴트를 만들 수 있습니다. 또한 예를 들어 정보 검색과 다양한 콘텐츠 요약을 통해 대화형 검색을 가능하게 하는 챗봇을 만들 수 있습니다. 따라서 사용 사례를 확인한 후에는 데이터가 필요합니다. 이제 사람들은 데이터가 새로운 석유라고 말합니다.

석유처럼 올바른 데이터를 검색하고 찾은 다음 올바른 프로세스를 사용하여 다양한 제품으로 변환한 다음 다양한 프로세스에 동력을 공급해야 하기 때문입니다. 데이터 사이언티스트가 가장 중요하게 생각하는 것은 정형 데이터, 일명 테이블 데이터인 경우가 많습니다. 참고로 데이터 사이언티스트는 여전히 구조화되지 않은 데이터를 다루지만 AI 엔지니어만큼은 아닙니다. 이제 이러한 테이블은 수백 개에서 수십만 개의 관측치 정도인 경우가 많으며 데이터를 모델링하려면 많은 정리 및 사전 처리가 필요합니다. 예를 들어, 일부 정리 작업에는 이상값을 제거하거나, 새 테이블을 조인 및 필터링하거나, 완전히 새로운 기능을 생성하는 작업이 포함됩니다.

이렇게 정리된 데이터는 다양한 기계 학습 모델을 학습하는 데 사용됩니다. 반면에 AI 엔지니어는 주로 텍스트, 이미지, 비디오, 오디오 파일 등과 같은 구조화되지 않은 데이터를 가장 많이 선택합니다. LLM, 즉 Large Language Model이라는 텍스트 기반 모델을 예로 들어 보겠습니다. 이러한 모델을 학습하려면 수십억에서 수조 개의 텍스트 토큰이 필요하며, 이는 기존 기계 학습 모델에 비해 훨씬 더 큰 규모입니다. 이것은 다음 차이점 영역으로 이어집니다.

따라서 데이터 과학 툴박스는 수백 개의 서로 다른 모델과 선택할 수 있는 다양한 알고리즘으로 구성됩니다. 이러한 모델의 특성상 사용 사례마다 다른 데이터 세트를 수집해야 하므로 다른 모델을 학습해야 합니다. 결과적으로 이러한 개별 모델의 범위는 훨씬 더 좁아집니다. 즉, 학습된 데이터 영역을 넘어서 일반화하기가 더 어려워집니다. 그리고 일반적으로 이러한 모델은 파라미터 수 측면에서 크기가 훨씬 작고, 학습과 추론을 수행하는 데 필요한 컴퓨팅 파워가 적고, 훈련에 필요한 시간도 몇 초에서 몇 시간 사이입니다.

반면, 제너레이티브 AI 툴박스는 훨씬 덜 복잡합니다. 그리고 실제로는 한 가지 유형의 모델만 포함하는데, 이를 기초 모델이라고 합니다. 이제 기초 모델은 한 가지 유형의 모델을 재교육 없이 광범위한 작업에 일반화할 수 있기 때문에 혁신적입니다. 이러한 모델은 정교하기 때문에 크기가 훨씬 더 크고, 종종 수십억 개의 파라미터가 있고, 훈련에 훨씬 더 많은 컴퓨팅 파워가 필요합니다. 즉, 수백 개에서 수천 개의 GPU에 대해 이야기하고 있으며, 훨씬 더 많은 교육 시간이 필요합니다.

이제 우리는 몇 주에서 몇 달 사이에 이야기를 나누고 있습니다. 이는 기존 머신 러닝 모델과 기반 모델 간의 본질적 특성의 차이로 인해 이러한 모델을 사용하여 솔루션을 개발하는 데 사용되는 기본 프로세스와 기법도 서로 다르다는 의미이기도 합니다. 따라서 일반적인 데이터 과학 프로세스는 다음과 같습니다. 먼저 사용 사례로 시작한 다음 해당 사용 사례에서 적절한 데이터를 선택합니다. 그런 다음 데이터가 준비되면 이를 사용하여 특징 엔지니어링, 교차 검증 또는 하이퍼파라미터 조정과 같은 기법을 사용하여 모델을 훈련하고 검증합니다.

그런 다음 이 모델을 클라우드와 같은 일부 엔드포인트에 배포하여 실시간 예측 및 추론을 수행합니다. 반면, 제너레이티브 AI 프로세스도 사용 사례로 시작하지만, 그 다음에는 사전 학습된 모델로 바로 넘어갈 수 있습니다. 이를 가능하게 하는 것은 AI 민주화라고 불리는 현상입니다. 이는 단순히 일상 사용자들이 AI에 더 널리 접근할 수 있도록 한다는 의미의 큰 공상적 단어입니다. 최고의 기반 모델 중 일부는 Hugging Face와 같은 오픈 소스 커뮤니티에 게시됩니다.

이러한 모델은 매우 일반화할 수 있고 즉시 사용할 수 있는 매우 강력하기 때문에 개발자가 쉽게 시작할 수 있습니다. AI 엔지니어는 자연어 명령을 통해 이러한 기초 모델과 상호 작용하여 다양한 작업을 수행하도록 유도하는데, 이 프로세스를 프롬프트 엔지니어링이라고 합니다. 이제 프롬프트 엔지니어링을 다양한 프레임워크와 함께 사용하여 더 큰 AI 시스템을 구축할 수 있습니다. 이러한 프레임워크의 예로는 여러 프롬프트를 하나로 연결하거나, 도메인별 데이터에 대해 파라미터 효율적인 미세 조정 (PEFT) 이라고 하는 작업을 수행하거나, 정확한 해답을 찾기 위해 검색-증강 생성 (일명 RAG) 을 수행하거나, 매우 복잡한 다단계 문제를 추론할 수 있는 자율 에이전트를 만드는 것이 포함됩니다. 따라서 이것들은 대규모 AI 애플리케이션을 구축하는 데 사용할 수 있는 빌딩 블록의 몇 가지 예일 뿐입니다.

마지막 단계는 AI를 대규모 시스템 또는 워크플로에 내장하는 것입니다. 이는 어시스턴트나 가상 에이전트를 만들거나, UI를 사용하여 더 큰 애플리케이션을 구축하거나, 일종의 자동화를 수행하는 형태를 취할 수 있습니다. 자, 이제 한 걸음 물러나서 모든 차이점을 아주 높은 수준에서 살펴봅시다. 보시다시피, 제너레이티브 AI의 혁신은 데이터 사이언티스트와 AI 엔지니어가 작업하는 사용 사례, 데이터, 모델 및 프로세스의 많은 차이점을 뒷받침합니다. 두 필드 간에는 여전히 겹치는 부분이 있다는 점에 유의하는 것이 중요합니다.

예를 들어 데이터 과학자는 여전히 규범적 사용 사례를 연구하거나 AI 엔지니어는 구조화된 데이터를 계속 연구할 것입니다. 이러한 차이점에도 불구하고 이 두 분야는 매일 새로운 연구 논문, 새로운 모델, 새로운 도구가 나오면서 눈부신 속도로 계속 발전하고 있습니다. 데이터, AI, 창의적인 마인드가 있다면 이것들로 정말 모든 것이 가능합니다. 이 정보가 도움이 되었기를 바랍니다.

## 예시
- 첫 번째 차이점은 사용 사례에 있습니다.
- 그래서 예측 가능한 사용 사례가 필요하죠.
- 이제 AI 엔지니어링에 대해 설명하자면, AI 엔지니어가 연구하는 주요 사용 사례 중 하나는 규범적 사용 사례라고 불리는데, 이는 모두 최선의 행동 방침을 선택하는 것입니다.
- 규범적 사용 사례의 또 다른 예는 추천 엔진을 만드는 것입니다.

## 요약
- 반면에 AI 엔지니어는 AI 시스템 빌더라고 생각하시면 됩니다.
- 이제 AI 엔지니어링에 대해 설명하자면, AI 엔지니어가 연구하는 주요 사용 사례 중 하나는 규범적 사용 사례라고 불리는데, 이는 모두 최선의 행동 방침을 선택하는 것입니다.
- 따라서 이것들은 대규모 AI 애플리케이션을 구축하는 데 사용할 수 있는 빌딩 블록의 몇 가지 예일 뿐입니다.

<details>
<summary>영문 Transcript 원문</summary>

For many years, data science has been called the sexiest job of the 21st century, but in recent years, it seems like there's a new job vying for that title, the AI engineer. So who even are these new kids on the block? Are they just data scientists in disguise? I'm Isaac Key, and I'm a former data scientist turned AI engineer at IBM. To answer these questions, I'm going to lay out four key areas in which the work of a data scientist differs from an AI engineer, specifically a generative AI engineer.

But before I dive into these differences, we first have to understand more about what's happening in the industry. So traditionally, data scientists have always used AI models to do their analysis. Well, with the advent of generative AI, the boundaries of what AI can do are being pushed in ways that we've never seen before. And so these breakthroughs have been so groundbreaking that generative AI has split off into its own distinct field, and we call that AI engineering. Okay, so now that we understand the landscape, let's dive into the differences.

The first area of difference lies in the use cases. So at a very high level, think of a data scientist as a data storyteller. They take massive amounts of messy real world data, and they use mathematical models to translate this data into insights. On the other hand, think of an AI engineer as an AI system builder. They use foundation models to build generative AI systems that help to transform business processes.

So since data scientists are fantastic storytellers, they use a lot of descriptive analytics to describe the past. One example of this is through what's called exploratory data analysis, or EDA, which is all about graphing the data and doing statistical inference. They can also do this through what's called clustering, which group similar data points based off of similar characteristics, such as, say, doing customer segmentation. Now every good story has a reader trying to figure out what's going to come next, and that's where predictive use cases comes in. As opposed to a book, however, a data scientist does not have the end already written, so they have to use what are called machine learning models to make their predictions.

An example of this is called regression models, which predict a numeric value such as, say, a temperature or a revenue. Another type of these models are classification models, which predict a categorical value such as a success or a failure. So putting on the AI engineering hat now, one of the main use cases that AI engineers work on are called prescriptive use cases, which are all about choosing the best course of action. An example of this is a technique called decision optimization, which enables businesses to assess a set of possible actions and then choose the most optimal path based off a set of requirements or standards. Another example of a prescriptive use case is through creating what are called recommendation engines.

As an example, this can involve suggesting targeted marketing campaigns for a select customer base. In addition to prescriptive use cases, there are also generative use cases, hence the name generative AI. Now foundation models, which I will touch on more in a bit, enable the creation of what are called intelligent assistants, for example, a coding assistant or a digital advisor. They also enable the creation of chatbots, as an example, which enable conversational search through information retrieval and the summarization of various content. So after we have a use case identified, we need data.

Now people say that data is a new oil because like oil, you have to search for and find the right data and then use the right processes to transform it into various products, which then power various processes. For a data scientist, the oil of choice is often structured data, aka tabular data. Do note that data scientists still work with unstructured data, but not as much as AI engineers. Now these tables are often in the order of hundreds to hundreds of thousands of observations, and they require a lot of cleaning and preprocessing before the data can be modeled. Some of the cleaning involved, for example, involves removing outliers or joining and filtering on a new table, or even creating new features altogether.

This clean data is then used to train various machine learning models. Now on the other hand, an AI engineer, for them, the oil of choice is mainly unstructured data such as text, images, videos, audio files, etc. Let's take a text-based foundation model called an LLM, or Large Language Model, as an example. These models require anywhere between billions to trillions of tokens of text to be trained on, which is a lot larger scale compared to traditional machine learning models. This leads me to the next area of difference, which is the underlying models.

So the data science toolbox consists of hundreds of different models and different algorithms that they can choose from. Due to the nature of these models, each different use case requires gathering a different dataset, and thus requires training a different model. And so as a result, the scope of these individual models is a lot more narrow, meaning that it's harder for them to generalize past the domain of data that they've been trained on. And generally speaking, these models are a lot smaller in size in terms of the number of parameters, they take less compute power to train and do inference, and they require less time to train, anywhere between seconds to hours. Now on the other hand, the generative AI toolbox is a lot less cluttered, and it really only contains one type of model, and that is called the foundation model.

Now foundation models are revolutionary because they allow for one single type of model to generalize to a wide range of tasks without having to be retrained. Thus their scope is called more wide. And due to the sophistication of these models, they are a lot larger in size, often billions of parameters, they require a lot more compute power to train, we're talking hundreds to thousands of GPUs, and they require a lot more training time. Now we're talking anywhere between weeks to months. Due to the differences in the intrinsic nature between traditional machine learning models and foundation models, this also means that the underlying processes and techniques that are used to develop solutions with these also differ.

So a typical data science process will look something like this. You start off with a use case, and then from that use case, you pick the right data. Then after that data is prepared, you use it to train and validate a model using techniques such as feature engineering, cross-validation, or hyperparameter tuning, as an example. This model then is deployed at some endpoint, for example in the cloud, to do real-time prediction and inference. Now on the other hand, the generative AI process also starts off with a use case, but then we can skip directly to working with a pre-trained model.

And what makes this possible is a phenomenon called AI democratization, which is a big fancy word that simply means making AI more widely accessible to everyday users. Some of the best foundation models out there are published to open-source communities such as Hugging Face, and since these models are so generalizable and so powerful out of the box, they make it easy for developers to get started. AI engineers interact with these foundation models via natural language instructions to prompt them to do various tasks, and this process is known as prompt engineering. Now prompt engineering can be used in conjunction with different frameworks to then build larger AI systems. An example of these frameworks include, as one, chaining different prompts together, or doing what's called parameter-efficient fine-tuning, or PEFT, on domain-specific data, or doing retrieval-augmented generation, aka RAG, to ground answers in truth, or even by creating autonomous agents to reason through very complex multi-step problems.

So these are just a few of the examples of the building blocks that can be used to build larger AI applications. The last step is to then embed the AI in a larger system or workflow. This can take on the form of creating assistants or virtual agents, building a larger application with a UI, or even doing some sort of automation. So okay, let's take a step back and let's look at all the differences at a very high level. As we can see, the breakthroughs in generative AI underpin many of the differences in the use cases, data, models, and processes that data scientists and AI engineers work on.

It's important to note that there is still overlap between the two fields. For example, data scientists will still work on prescriptive use cases, or an AI engineer will still work with structured data. Regardless of these differences, both of these fields are continuing to evolve at a blazing fast pace with new research papers, new models, new tools coming out every single day. With data, AI, and a creative mind, really anything is possible with these. Thank you for tuning in.

I hope this was helpful. Until next time, peace.

</details>
