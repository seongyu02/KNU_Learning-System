# (Optional Review) Odometry (Chapter 13.4)

## 개요

- 이 항목은 **캡스톤을 위한 선택 복습(optional review) 영상**이며, 앞선 강좌의 강의와 **동일한 영상**이다.
- 정리본 원본: **Odometry (Chapter 13.4)**
- 캡스톤에서의 역할: 마일스톤 1의 시뮬레이터 핵심. 바퀴 속도로부터 섀시 구성을 갱신한다

## 내용

### 복습 포인트

- 절차: `Δθ` 측정 → `Δt = 1` 로 두어 `θ̇ = Δθ` → `V_b = F Δθ` → 행렬 지수로 적분 → 공간 프레임 표현
- 전방향 로봇은 `F = H(0)⁺`. 메카넘 4륜은 3 × 4 의사역행렬
- `T_{s,b_{k+1}} = T_{s,b_k} e^{[V_b6]}` 로 갱신하고 `q_{k+1}` 을 추출한다

## 요약

- 새로운 내용이 없는 **복습 영상**이므로 정리본을 중복 작성하지 않고 원본 노트로 연결한다.
- 원본: **Odometry (Chapter 13.4)**
