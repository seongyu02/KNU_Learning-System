# (Optional Review) Mobile Manipulation (Chapter 13.5)

## 개요

- 이 항목은 **캡스톤을 위한 선택 복습(optional review) 영상**이며, 앞선 강좌의 강의와 **동일한 영상**이다.
- 정리본 원본: **Mobile Manipulation (Chapter 13.5)**
- 캡스톤에서의 역할: 마일스톤 3의 야코비안 `J_e` 유도 그 자체

## 내용

### 복습 포인트

- `T_se(q,θ) = T_sb(q) T_b0 T_0e(θ)`
- `V_e = J_e(θ)[u; θ̇]`, `J_e = [J_base  J_arm]` (6 × (m+n))
- `J_base = [Ad_{T_e0 T_0b}] F₆`, `J_arm = J_b`
- `J_e` 는 `θ` 에만 의존하고 섀시 구성 `q` 와 무관하다
- `[u; θ̇] = J_e⁺(θ) V`

## 요약

- 새로운 내용이 없는 **복습 영상**이므로 정리본을 중복 작성하지 않고 원본 노트로 연결한다.
- 원본: **Mobile Manipulation (Chapter 13.5)**
