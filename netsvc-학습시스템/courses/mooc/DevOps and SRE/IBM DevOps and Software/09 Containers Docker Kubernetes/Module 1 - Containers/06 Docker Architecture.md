# Docker Architecture

> MOOC 영상 · [원본 강의](https://www.mooc.org/learn/ibm-containers-docker-kubernetes-openshift/lecture/5ZK8J/docker-architecture)

## 개요
- 먼저 Docker 명령줄 인터페이스를 사용하거나 Docker 클라이언트를 통해 REST API를 사용하여 Docker 호스트 서버에 명령을 보냅니다, 일반적으로 호스트라고 합니다.

## 내용
- 먼저 Docker 명령줄 인터페이스를 사용하거나 Docker 클라이언트를 통해 REST API를 사용하여 Docker 호스트 서버에 명령을 보냅니다, 일반적으로 호스트라고 합니다.
- 동일한 시스템에서 Docker 클라이언트와 데몬을 실행하거나 도커 클라이언트를 원격 도커 데몬에 연결할 수 있습니다 도커 데몬은 다른 데몬과 통신하여 도커 서비스를 관리할 수도 있습니다.
- 또한 레지스트리 위치는 IBM Cloud와 같은 타사 제공업체를 사용하여 호스팅되거나 컨테이너 레지스트리와 같은 타사 공급업체를 사용하여 호스팅하거나 프라이빗 데이터 센터 또는 클라우드에서 자체 호스팅합니다.
- 먼저 개발자는 자동화 또는 빌드 파이프라인을 사용하여 이미지를 빌드하고 푸시합니다 빌드 파이프라인을 사용하여 이미지를 빌드하고 Docker가 이러한 이미지를 저장하는 레지스트리로 푸시합니다.
- 다음은 Docker 아키텍처의 시각적 표현입니다, 클라이언트, Docker 데몬을 포함한 Docker 호스트, 그리고 기존에 저장된 이미지가 있는 레지스트리로 구성되어 있습니다.
- 호스트는 먼저 이미지가 이미 사용 가능한지 로컬에서 확인한 다음 그런 다음 이미지 이름으로 실행 명령을 실행하여 컨테이너를 만듭니다.
- 또한 도커 호스트는 이미지, 컨테이너, 네임스페이스, 네트워크, 네트워크, 스토리지, 플러그인 및 애드온도 관리합니다 컨테이너화는 이미지를 빌드, 푸시 및 실행하는 데 사용되는 프로세스입니다.

## 예시
- 또한 도커 호스트는 이미지, 컨테이너, 네임스페이스, 네트워크, 네트워크, 스토리지, 플러그인 및 애드온도 관리합니다 컨테이너화는 이미지를 빌드, 푸시 및 실행하는 데 사용되는 프로세스입니다.

## 요약
- 호스트는 먼저 이미지가 이미 사용 가능한지 로컬에서 확인한 다음 그런 다음 이미지 이름으로 실행 명령을 실행하여 컨테이너를 만듭니다. 또한 도커 호스트는 이미지, 컨테이너, 네임스페이스, 네트워크, 네트워크, 스토리지, 플러그인 및 애드온도 관리합니다 컨테이너화는 이미지를 빌드, 푸시 및 실행하는 데 사용되는 프로세스입니다.
