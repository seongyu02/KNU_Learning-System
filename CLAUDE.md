# CLAUDE.md

이 저장소에는 **두 시스템**이 나란히 있다. 섞지 않는다.

| 경로 | 무엇인가 | 다루는 법 |
|---|---|---|
| `내과제/` · `.claude/skills/오늘/` | **과제 마감 비서.** 내가 만든 시스템 | 여기서 일한다 |
| `netsvc-학습시스템/` | 수업에서 **배포받은 학습 시스템** | 출발점. 고치지 않는다. 자체 `CLAUDE.md` 가 있다 |

배포본의 지침·스킬·기록 구조를 출발점으로 삼아 **옆에** 지었다.
공부하는 맥락(`/study`, `/roadmap`)이면 `netsvc-학습시스템/` 안에서 일하고,
그 폴더의 `CLAUDE.md` 규칙을 따른다.

지금 사용자는 **강남대학교 인공지능융합공학부 학부생**이고, 2026년 2학기에 7과목을 듣는다.

## 이 저장소에서 지키는 것

- **한국어로 답한다.** 기술 용어는 필요할 때 영어를 병기한다.
- 진행 기록은 반드시 **파일로 남긴다.** 대화는 사라지고 파일은 남는다.
- **모르는 것을 추정해서 채우지 않는다.** 마감일·소요시간·배점을 지어내지 않는다.
  없으면 "확인 대기"로 남기고 사용자에게 묻는다. 틀린 마감일은 없는 것보다 나쁘다.
- **개인 자격증명은 저장소에 올리지 않는다.** 이캠퍼스 달력 URL과 웹앱 토큰은
  `내과제/.env.local` 에 있고 `.gitignore` 되어 있다. 이 파일 내용을 대화에 출력하지 않는다.

## 폴더

| 경로 | 용도 |
|---|---|
| `내과제/` | 과목 · 마감목록 · 실행기록. **기록의 원본은 여기다** |
| `.claude/skills/오늘/` | `/오늘` — 오늘 할 과제를 정하는 행동 매뉴얼 |
| `deploy/` | Cloudflare Workers 배포. 서버가 이캠퍼스 일정을 직접 받아온다 |
| `netsvc-학습시스템/` | 배포받은 학습 시스템. 읽기 전용으로 취급한다 |

## 스킬을 먼저 쓴다

- "뭐부터 하지" "오늘 뭐 해야 돼" "과제 뭐 남았어" → **`/오늘`**
- "일정 동기화해줘" "새 과제 있나" "마감 업데이트" → **`/동기화`**
- "공부하자" "시험 봐 줘" → `netsvc-학습시스템/` 의 **`/study`**

스킬을 흉내 내지 말고 실제로 호출한다.

## 화면과 배포를 건드릴 때

전부 실제로 깨져 보고 알아낸 것들이다. 고치기 전에 읽는다.

**화면은 한 벌만 관리한다.** `내과제/웹앱.html` 이 원본이고, Claude 아티팩트와
Cloudflare Worker 가 같은 파일을 쓴다. 고쳤으면 **양쪽에 다 올린다.**

```bash
python deploy/build.py     # 웹앱.html -> deploy/public/app.html
cd deploy && npx wrangler deploy
```
아티팩트는 Artifact 도구로 같은 파일 경로를 다시 올린다. 한쪽만 올리면 갈라진다.

**어댑터가 화면보다 먼저 와야 한다.** `worker.js` 의 `shell()` 에서 `SHIM` 을
fragment 앞에 둔다. 화면은 로드 즉시 `window.claude` 를 확인하는데, 뒤에 두면
그 시점에 없어서 **화면은 뜨는데 데이터만 영영 안 붙는다.**

**ID 규칙은 세 곳이 같아야 한다.** `일정가져오기.py` · `웹앱.html` · `worker.js` 가
같은 FNV-1a 해시를 낸다. 하나만 고치면 같은 과제가 두 개로 갈라진다. 고쳤으면 검증한다.

```bash
node deploy/test.mjs <일정.ics 경로>
```

**우선순위 판단은 `/오늘` 스킬에만 둔다.** 화면(`웹앱.html`)은 마감순으로 보여줄 뿐
가용시간을 묻지도, 3개로 줄이지도 않는다.

한때 같은 계산을 화면에도 넣었다가 걷어냈다. 웹에는 Claude 가 없어 스킬을 읽을 수
없으니 자바스크립트로 옮겨 적어야 했는데, **그 순간 같은 규칙이 두 벌이 된다.**
한쪽만 고치면 대화와 화면이 다른 답을 내고, 어느 쪽이 맞는지 사용자는 알 수 없다.

> **판단을 화면에 다시 넣지 않는다.** 넣으려면 두 벌을 맞출 방법부터 정한다.
> 화면은 보여주고, 판단은 스킬이 한다 — 스킬만 **되물을 수 있기** 때문이다.

**한글은 NFC 로 모은다.** 이캠퍼스는 제목 일부를 NFD(분해형)로 내보낸다.
눈에는 한글인데 프로그램에게는 자모 낱개라 식별자가 통째로 빈다.

**`.bat` 은 ASCII 로만 쓴다.** cmd.exe 가 콘솔 코드페이지로 읽어서 한글이 있으면
명령이 깨진다. 한글 경로 처리는 `sync.py` 가 맡는다.

**비밀값은 두 곳에만 있다.** `내과제/.env.local`(로컬, gitignore)과
`wrangler secret`(서버, 암호화). 저장소·대화·생성된 HTML 어디에도 넣지 않는다.

## 기록

과제를 관리했으면 `내과제/실행기록/YYYY-MM-DD.md` 에 남기고 커밋을 권한다.
공부했으면 `netsvc-학습시스템/내학습/진행상황.md` 에 남긴다.

## 사용자에게 물어야 하는 결정

내가 정하지 않는다.

- 같은 날 마감이 겹칠 때 무엇을 먼저 할지
- 과제를 포기하거나 연기할지
- 예상 소요시간이 얼마인지
- 자동 수집이 "과제 아님"으로 분류한 것이 사실 과제인지
